# Architecture & Reuse Registry

> One line per durable thing. Edit the line when it changes; delete it when the thing is gone.
> No story narrative, no dates, no rationale.

## Shared utilities & components

| Export | Location | Capability |
| --- | --- | --- |
| `apiClient`, `get`, `post`, `put`, `del` | `web/src/lib/api/client.ts` | The only HTTP surface. Same-origin paths by default; `baseUrl` in the request config addresses a service directly for server-side calls; sends no credential (cookie-session only). Reads a failed response's own wording from either body shape (`Messages[]` or `{ Error, Message }`). |
| `isAPIError`, `statusCodeOf`, `serviceMessageOf`, `serviceDetailOf`, `CLIENT_FALLBACK_MESSAGES`, `CLIENT_FALLBACK_DETAILS` | `web/src/lib/api/errors.ts` | Reading a failed API call: its status, and the message the service actually sent — `undefined` when all that came back was a client-side placeholder, which no user should be shown. The service's wording can land in either of two places, so both are readable: `serviceMessageOf` for `message`, `serviceDetailOf` for `details` (which is where a 500's `Messages[]` ends up, `message` holding the client's placeholder). Pair them: `serviceMessageOf(e) ?? serviceDetailOf(e) ?? <own wording>` — every screen reading a failed call does. Both placeholder sets (`CLIENT_FALLBACK_MESSAGES` for `message`, `CLIENT_FALLBACK_DETAILS` for `details`) are declared here and imported BY the client, so the wording the client writes and the wording these readers recognise can never drift apart. |
| `API_BASE_PATH`, `AUTH_API_BASE_PATH`, `TRANSACTIONS_API_BASE_PATH`, `AUTH_ENDPOINTS`, `SIGNED_IN_HOME_ROUTE`, `SIGN_IN_ROUTE`, `SESSION_ENDED_PARAM`, `SESSION_TIMED_OUT_REASON`, `SIGN_IN_TIMED_OUT_ROUTE`, `PAGINATION` | `web/src/lib/utils/constants.ts` | Same-origin API paths for both backends, the auth service's login/logout/userinfo paths, and the session routes — usable from browser code, unlike the server-only gate. Includes the sign-in address that asks the screen to explain a session that ended on its own, and the query parameter it reads. `PAGINATION` is the project's paging vocabulary for every paged list — a default page size of 20 chosen from 5 / 10 / 20 / 50 (the starter template's 25 from 10/25/50/100 was replaced, not shadowed); stated once, so a selector and the rows it sizes cannot disagree. |
| `UserInfoRead`, `RoleRead`, `ROLE_IMPORTER`, `ROLE_APPROVER`, `PROJECT_ROLES`, `ProjectRole` | `web/src/types/auth.ts` | The identity/role contract and the two project role names — single source of truth, re-exported by the `src/mocks/data/` factories. The names are the auth service's own wire values (`Importer`, `Approver`), verified against the live service; `Importer` is the role the requirements call "Finance Uploader", and authorization matches the wire value, never the requirements' wording. |
| `requireSession`, `SIGN_IN_ROUTE` (re-export) | `web/src/lib/auth/requireSession.ts` | Server-side gate for protected layouts and pages: resolves the current identity or redirects to the sign-in screen. Resolves once per server request, so a layout and the page inside it cost the auth service one identity call; nothing is held between requests. |
| `fetchUserInfo` | `web/src/lib/auth/authApi.ts` | Server-side auth-service access: identity lookup that forwards the session cookie (`null` on 401/403, throws on real failures). |
| `authApiBaseUrl`, `transactionsApiBaseUrl` | `web/src/lib/api/serviceBaseUrl.ts` | Each backend's address, resolved from configuration on every call (never a module constant): `AUTH_API_BASE_URL` / `TRANSACTIONS_API_BASE_URL` first, then the `NEXT_PUBLIC_*` names, then the documented localhost fallback. Server-side only — the server-only names are preferred because `next build` inlines every `NEXT_PUBLIC_*` read as a literal. |
| `forwardPathToService`, `forwardToService`, `BACKEND_UNREACHABLE_ERROR` | `web/src/lib/api/serviceProxy.ts` | Server-side transport that carries a same-origin call to the backend that owns it: forwards method, body and headers, drops the hop-by-hop set plus a stale `host`/`content-length`, adds nothing of its own (no credential), passes every `Set-Cookie` back individually via `getSetCookie()`, and answers an unreachable service with a 502 carrying a code and no wording. `forwardPathToService(request, prefix, segments)` is what a mount's route handler calls: it resolves the catch-all segments against `prefix` through the URL parser and refuses with a 404 anything that normalises outside it (the segments arrive decoded, so `..` would otherwise walk out of the mount point). Not for application code, which calls `lib/api/client.ts`. |
| `SESSION_COOKIE_NAME`, `sessionCookieOptions`, `clearedSessionCookieOptions`, `sessionCookieHeader` | `web/src/lib/auth/sessionCookie.ts` | The session cookie's name and attributes; set and cleared forms derive from one place so they always match. |
| `isProjectRole`, `rolesOf`, `hasRole`, `RoleBearer` | `web/src/lib/auth/roles.ts` | Role checks against the current userinfo response; unrecognised role names grant nothing. `RoleBearer` is the minimum identity a permission decision needs. |
| `ACCESS_MAP`, `accessEntryFor`, `canAccess`, `entryPointsFor`, `LANDING_PATH`, `UPLOAD_PATH`, `REQUESTS_PATH`, `SUBMITTED_FILE_PATH` | `web/src/lib/auth/access-map.ts` | The one route access map: every registered address, the roles that may open it, the permission it needs (worded as requirements §6.5 names it), and the entry points to offer a session. Unregistered addresses grant nothing. An entry with no `entryPoint` copy is openable but never advertised — that is how a screen reached from a record's row (the submitted file at `SUBMITTED_FILE_PATH`) stays out of the landing screen and the header navigation, both of which render exactly what the map offers. `LANDING_PATH` is not a screen for everyone: `app/(authenticated)/page.tsx` redirects a session holding exactly ONE recognised role to that role's screen (`REQUESTS_PATH` for Approver, `UPLOAD_PATH` for Importer) before any markup, so the entry points are only ever seen by a session holding both roles or neither — anything that links to `LANDING_PATH` (the header's app name) lands such a person on their own screen. Nothing may redirect back the other way. |
| `signIn`, `signInFailureMessage`, `SIGN_IN_REFUSED_MESSAGE`, `SIGN_IN_UNAVAILABLE_MESSAGE` | `web/src/lib/auth/signInApi.ts` | Browser-side sign-in (so the auth service's `Set-Cookie` reaches the browser), plus what to tell the user when it fails: the service's reason as given, else a refusal or unavailable message. |
| `signOut`, `signOutFailureMessage`, `SIGN_OUT_FAILED_TITLE`, `SIGN_OUT_UNAVAILABLE_MESSAGE` | `web/src/lib/auth/signOutApi.ts` | Browser-side sign-out: resolves only once the auth service confirms the session ended (rejects otherwise), plus what to tell the user when it fails. |
| `displayNameOf`, `roleLabelOf`, `actingUploaderIn` | `web/src/lib/auth/identity.ts` | How a person reads on screen: their name from the identity parts, and a role label listing only roles this project recognises. `actingUploaderIn` is the SERVER-side decision about who may act on a submitted file — the signed-in Finance Uploader's own name, `undefined` for anybody else — stated once for the two pages that make it (`/upload` and `/upload/file`), so the list and the file's own page cannot gate differently. Its absence is what leaves an uploader-only action out of the markup rather than rendering it disabled, and the same value is the `LastChangedUser` audit identity those calls send. |
| `signInSchema`, `SignInValues`, `REQUIRED_CREDENTIALS_MESSAGE`, `expenseFileSubmissionSchema`, `ExpenseFileSubmissionValues`, `isCsvFileName`, `CSV_ONLY_MESSAGE`, `FILE_SETTING_REQUIRED_MESSAGE`, `FILE_REQUIRED_MESSAGE`, `rejectionNoteSchema`, `RejectionNoteValues`, `REJECTION_NOTE_REQUIRED_MESSAGE` | `web/src/lib/validation/schemas.ts` | The project's Zod schemas and the wording each rule reports: sign-in credential presence, one expense-file submission (a chosen setting plus a file name that must identify a CSV — the check is on the NAME, never the browser-reported content type), and the reason a rejection has to carry. The rejection note is trimmed BEFORE its presence check, which is the whole of the rule as written ("empty or whitespace-only") and also makes the value that reaches the service the words the user wrote rather than the spaces around them. |
| `AppHeader`, `HeaderNav`, `UserMenu`, `SignOutButton`, `ThemeToggle` | `web/src/components/layout/` | The signed-in shell's `banner` header: the app name as the link to `LANDING_PATH`, the navigation to every screen the session may open, the current session's name and role, the user menu holding sign-out, and the light/dark switch (one button, its accessible name naming the version it switches to). `HeaderNav` is the only client part (it reads `usePathname()` to mark the viewed screen `aria-current="page"`, tolerating `null` as "not known yet") and takes its destinations and their wording from `entryPointsFor`. Shell components and future header controls belong in this folder. |
| `Theme`, `THEME_STORAGE_KEY`, `DARK_THEME_CLASS`, `THEME_CHANGE_EVENT`, `storedTheme`, `systemTheme`, `resolveTheme`, `activeTheme`, `applyTheme`, `rememberTheme`, `subscribeToTheme`, `THEME_INIT_SCRIPT` | `web/src/lib/theme/theme.ts` | The one place the light/dark decision lives — a remembered choice first, else the computer's `prefers-color-scheme` — shared by the root layout's before-paint script (`THEME_INIT_SCRIPT`) and the header switch, so they cannot drift apart. Also exposes the active version as external state React can watch: `activeTheme` reads it back off `<html>`, `subscribeToTheme` reports every change (the switch, or the computer's setting changing mid-session). Anything that needs to show the current version pairs those two in `useSyncExternalStore`. |
| `VISIBILITY_CHANGE_EVENT`, `subscribeToPageVisibility`, `isPageVisible`, `isPageVisibleOnServer` | `web/src/lib/layout/pageVisibility.ts` | Whether the reader can actually see this page, as external state React watches (`useSyncExternalStore`), for anything that must stop happening while the tab is in the background — a timed re-read being the first. Only `hidden` counts as hidden, so a value the browser adds later reads as visible rather than as absent. The server snapshot is "the reader is here": nothing rendered depends on it, and it decides only whether a browser-side timer runs. |
| `NARROW_VIEWPORT_QUERY`, `subscribeToViewportWidth`, `isNarrowViewport`, `isNarrowViewportOnServer` | `web/src/lib/layout/viewport.ts` | Whether the reader is narrower than Tailwind's `md` breakpoint (768px, stated once here so a JavaScript branch and a CSS one cannot disagree), as external state React watches (`useSyncExternalStore`). For the case where a narrow screen needs a genuinely DIFFERENT presentation rather than a re-flowed one, so only one of the two is ever in the markup. The server snapshot is the honest "not narrow". |
| `RoleEntryPoints` | `web/src/components/dashboard/RoleEntryPoints.tsx` | The landing screen's role-aware entry points: one navigational link per address the session's roles allow, read from the access map. Excluded entry points are absent from the markup, never disabled. |
| `ConfirmAction` | `web/src/components/common/ConfirmAction.tsx` | The project's ONE confirmation for an action that has to be agreed to before it happens (source UI-09): the Shadcn `alert-dialog`, whose `AlertDialogCancel` holds initial focus for free — which is why it is that primitive and not the plain `dialog`, since a stray Enter must back out rather than act. CONTROLLED, with no trigger slot: a confirmation is reached from a plain button on one screen and from a per-request control in a list row on another, so the caller renders whatever asks and holds what is being confirmed. The caller's two labels must not read alike (the control that asks, the one that does it and the one that backs out are three different phrases), and the title NAMES the object — never "this file"/"this request". Nothing here calls anything: `onConfirm` is the caller's, and a refusal is reported on the screen BEHIND the dialog, which closes as the answer is given. |
| `PermissionDeniedMessage` | `web/src/components/auth/PermissionDeniedMessage.tsx` | The in-page denial any screen renders when the session's roles exclude its address: names the missing permission and how to request it, and links back to the landing screen. |
| `FileLog`, `FileLogList`, `FileProcessLog`, `FileProcessLogList`, `ValidationErrors`, `ValidationErrorRow`, `FileSettingRead`, `FileSettingReadList`, `FILE_STATUS_*`, `FILE_STATUSES`, `FileStatus`, `isKnownFileStatus`, `IN_PROGRESS_FILE_STATUSES`, `isFileInProgress` | `web/src/types/files.ts` | The transactions service's file contract and the five file-status names — the single source of truth the `src/mocks/data/` file factories re-export. `CurrentStatus` stays a free-form string (an unrecognised value is displayed as received, never remapped); `RecordCount` and `HasBulkErrorFile` are strings on the wire. `isFileInProgress` says whether a file's row is still expected to change. A processing activity still running carries neither `DecisionResult` nor `EndDate`. `ValidationErrors.JsonArray` is a JSON array delivered as a STRING, and every field of `ValidationErrorRow` is optional with `Amount` typed `number \| string` — a row rejected for a missing reference or a non-numeric amount carries exactly what the source file held; its `ErrorColumn` / `ErrorMessage` defect pair is INFERRED, not documented, and this file plus `src/mocks/data/validation-error.ts` are the only two places to change if a live response names them differently. |
| `FILE_LOGS_ENDPOINT`, `FILE_SETTINGS_ENDPOINT`, `FILE_UPLOAD_ENDPOINT`, `FILE_PROCESS_LOGS_ENDPOINT`, `FILE_VALIDATION_ERRORS_ENDPOINT`, `FILE_DOWNLOAD_ENDPOINT`, `FILE_BULK_ERRORS_DOWNLOAD_ENDPOINT`, `FILE_RETRY_VALIDATION_ENDPOINT`, `FILE_DELETE_ENDPOINT`, `fetchSubmittedFiles`, `fetchFileSettings`, `uploadExpenseFile`, `fileProcessingHistoryEndpoint`, `fetchFileProcessingHistory`, `fetchFileValidationErrors`, `rejectedRowsIn`, `downloadSubmittedFile`, `downloadGeneratedErrorFile`, `retryFileValidation`, `deleteSubmittedFile`, `uploadFailureMessage`, `UPLOAD_FAILED_MESSAGE`, `fileLookupFailureMessage`, `FILE_LOOKUP_FAILED_MESSAGE`, `processingHistoryFailureMessage`, `PROCESSING_HISTORY_FAILED_MESSAGE`, `validationErrorsFailureMessage`, `VALIDATION_ERRORS_FAILED_MESSAGE`, `downloadFailureMessage`, `DOWNLOAD_FAILED_MESSAGE`, `retryFailureMessage`, `RETRY_FAILED_MESSAGE`, `deleteFailureMessage`, `DELETE_FAILED_MESSAGE`, `ExpenseFileSubmission`, `SubmissionSetting` | `web/src/lib/api/files.ts` | The transactions service's file endpoints, addressed same-origin through the shared client. The list read carries the `IsActive=Yes` the contract requires, stated here so no screen can forget it. The settings read takes no parameters, so it answers with retired settings mixed in. The upload sends `FileSettingId` / `FileSettingName` / `FileName` as query parameters with the raw file as an `application/octet-stream` body (so not `post`, which would JSON-stringify it), and answers with an envelope carrying no file identifier — a submitted file is found by re-reading the list. One file's processing history takes its `LogId` as a PATH SEGMENT, not a query parameter, and answers with the array on a property called `FileLog` (not `FileProcessLog`) — both quirks are the contract's own. The rejected-rows read names its file with a `FileLogId` QUERY parameter and answers with the rows as a JSON string, so `rejectedRowsIn` is the one place that string is read: it answers `undefined` for every body that cannot be read as a list of rows (not a string, will not parse, parses to something that is not an array, or an element that is not an object) and an empty array where the service reported no rejected rows — a caller reports that as a state, never a throw. Nothing reads `/v1/files/validation-errors/columns`; column labels are the app's own wording. A file's two downloads are two DIFFERENT endpoints and the choice is made only here: the file as it was submitted comes from `/v1/files/download`, the generated error file from `/v1/files/bulk-errors/download`, both naming the file with a `FileLogId` query parameter and both asked for as a binary body (`isBinaryResponse`, so a streamed file is never read as JSON). The contract's third, similarly-shaped `/v1/file-logs/data` is used by NEITHER and nothing in this project may call it. The two MUTATING file calls name their file with a `LogId` query parameter (not the downloads' `FileLogId`) and both answer the generic envelope, which says nothing about the file's new state — a caller learns that by re-reading: retry is `POST /v1/files/retry-validation` with NO audit header, delete is `DELETE` on the BARE `/v1/files` path and REQUIRES `LastChangedUser`, taken from the authenticated identity and therefore a required argument rather than an option a caller can forget. `deleteSubmittedFile` is the app's ONE delete call, whatever surface offers it — a second wrapper or a second endpoint constant beside it is a bug — and what the service does to a file that has already IMPORTED is unverified, so a caller reports the answer rather than assuming the happy path. That header asymmetry is the contract's own; do not add one to the retry speculatively. Each `*FailureMessage` is what to tell the user when that call is refused, reading the service's own wording from either place a failure can carry it. |
| `onScreenDateTime` | `web/src/lib/utils/dateTime.ts` | How the app writes a moment it observed ITSELF: `YYYY-MM-DD HH:MM`, 24-hour, on the reader's own clock — the same notation the transactions service uses and every list prints verbatim, so an app-written time and a service-written one read alike. Seconds are left off (they belong only where two of something must be told apart, e.g. the export file name). A value the SERVICE sent never comes through here: it is printed exactly as it arrived, since its format is an unverified assumption. |
| `TransactionRead`, `TransactionReadList`, `TRANSACTION_STATUS_*`, `TRANSACTION_STATUSES`, `TransactionStatus`, `isKnownTransactionStatus` | `web/src/types/transactions.ts` | The transactions service's expense-payment-request contract and the three request-status names — the single source of truth the `src/mocks/data/transaction.ts` factory re-exports. `Status` and `TransactionType` stay free-form strings: the service owns both vocabularies, so an unrecognised value is displayed as received, never remapped or refused. `UserNote` is the only optional field (rejected requests carry it). |
| `TRANSACTIONS_ENDPOINT`, `fetchTransactions`, `transactionListFailureMessage`, `TRANSACTION_LIST_FAILED_MESSAGE` | `web/src/lib/api/transactions.ts` | The transactions service's expense-request endpoint, addressed same-origin through the shared client. The read takes NO query parameters — there is no server-side search, filter, sort or paging, so the whole set arrives at once and every narrowing happens in the browser over it. `transactionListFailureMessage` is what to tell the user when the read fails. Where later READ-ONLY transaction calls (an export) belong; deciding a request is `lib/api/decisions.ts`, because it cannot be a plain browser-to-service call. |
| `DECISIONS_ENDPOINT`, `DECISION_APPROVE`, `DECISION_REJECT`, `DecisionOutcome`, `isDecisionOutcome`, `DECIDE_SERVICE_PATHS`, `DecisionRequest`, `recordDecision`, `DECISION_FAILED_MESSAGE`, `decisionFailureMessage` | `web/src/lib/api/decisions.ts` | Recording one decision on one expense request: the two outcomes, the app's OWN address a decision is sent to (`/api/decisions`, never the service's path), and what to tell the user when it is refused. `recordDecision` takes NO `LastChangedUser` argument by design — the required audit header is resolved from the session by `app/api/decisions/route.ts`, so a caller has no name to supply and none to spoof. `DECIDE_SERVICE_PATHS` states the two service paths (`/v1/transactions/{approve,reject}`) once, for the two places that must agree: the route that forwards to them and the `/transactions-api/*` proxy that refuses to carry them. The answer is the generic envelope, which says nothing about the request's new status — a caller learns that by re-reading. |
| `TRANSACTION_TYPE_CREDIT`, `TRANSACTION_TYPE_DEBIT`, `transactionTypeLabel`, `lastFourDigitsOf` | `web/src/lib/transactions/display.ts` | The only two values of a request that are not printed as they arrived, stated once for every surface that shows them: the plain-language transaction-type label (the codes the app knows, and the service's own value verbatim for anything else — not a validation list) and the last four digits of an account number, which is all any list may show (POPIA). |
| `RequestNarrowing`, `NO_NARROWING`, `PickOneFilterField`, `RangeBoundField`, `NarrowingField`, `FilterChoice`, `AppliedNarrowing`, `RangeReport`, `withFilterValue`, `narrowRequests`, `appliedNarrowings`, `rangeReports`, `filterChoicesIn`, `STATUS_FIELD_NAME`, `FILE_FIELD_NAME`, `TRANSACTION_TYPE_FIELD_NAME`, `AMOUNT_RANGE_FIELD_NAME`, `DATE_RANGE_FIELD_NAME`, `AMOUNT_RANGE_WRONG_WAY_ROUND`, `DATE_RANGE_WRONG_WAY_ROUND`, `NARROWED_EMPTY_MESSAGE` | `web/src/lib/transactions/narrowing.ts` | How the expense request list is narrowed, stated once for the three surfaces that must agree — the controls, the summary of what is applied and the rows themselves. Holds what is applied (`''` means "not narrowing", so there is no separate on/off flag), which requests that leaves (several narrowings combine cumulatively), what is currently applied as a LIST (one entry per narrowing, so a new KIND of filter joins without reshaping anything), and the choices a pick-one filter offers — one per distinct value present in the fetched set, ordered by the wording the user reads, never a hardcoded enum. `withFilterValue` is the one channel every control changes its own value through, an exhaustive switch over `NarrowingField` so a new narrowing cannot be added without being handled. Search covers only the ON-SCREEN values, including just the visible last four digits of an account number: matching the unmasked value would be a way round the masking POPIA requires. The two-bound ranges (amount, transaction date) hold what the user TYPED and are read here: both bounds inclusive, either end may be left open, amounts compared as NUMBERS against `Amount` (by text order 9.99 falls inside 100–200), dates as whole DAYS against the leading `YYYY-MM-DD` of `TransactionDate` — which is what makes the upper bound cover its whole day, so a request stored with a time of day on the last day of a range is inside it. `TransactionDate` is never normalised (its format is an unverified assumption). A range whose upper bound is below its lower one applies NEITHER bound, contributes no summary entry, and comes back from `rangeReports` as wording naming it — never "keep the last valid bound", which would make the visible set depend on which keystroke arrived last. `NARROWED_EMPTY_MESSAGE` is the ONE sentence for "what is applied has hidden every request", stated here because two surfaces must say it and neither may invent its own: the rows' place, and the export explaining why activating it produced no file. Nothing here takes a query parameter, because the endpoint accepts none. |
| `RequestColumn`, `SortDirection`, `RequestSort`, `RequestColumnDefinition`, `REQUEST_COLUMNS`, `isRequestColumn`, `sortStateOf`, `nextSortFor`, `orderRequests`, `pageCountOf`, `pageOf`, `RecordRange`, `recordRangeOf`, `pageSizeFrom` | `web/src/lib/transactions/ordering.ts` | How the expense request list is ordered and cut into pages. `REQUEST_COLUMNS` is the one statement of the displayed columns — key, heading and the value each ORDERS by — so the heading row is rendered from it and every displayed column has a sort control by construction. Ordering is single-field, ascending on the first activation and descending on the second (`nextSortFor`), reported through `aria-sort` (`sortStateOf`, `'none'` on every other column). A column orders by what the row SHOWS, never by a value it hides: the account number by its visible last four digits (ordering by the full value would arrange the rows by something POPIA does not let the screen show), the transaction type by its plain-language label. An amount is compared as a NUMBER; everything else by code unit, matching a plain `Array.sort()` — not `localeCompare`, whose punctuation rules would differ from the fixed-width references and dates this data holds. Equal values keep their fetched order (stable sort). `pageCountOf` never answers below 1, so an empty set is still "page 1 of 1" and the page controls stay on screen. `recordRangeOf` describes that same slice in the reader's own numbering (from 1, both ends inclusive) for the listing's continuation line, and lives beside `pageOf` so the line and the rows it claims to describe are two readings of ONE calculation: the last page is CLAMPED to the set (page 22 of 428 at 20 a page ends at 428, not the naive 440) and an empty set holds records `0` to `0` rather than a first record it does not contain. Nothing here takes a query parameter — the endpoint accepts none. |
| `DECIDE_OUTCOMES`, `awaitsDecision`, `decideActionLabel`, `decideActionName`, `confirmationTitleFor`, `confirmationMessageFor`, `confirmDecisionLabel`, `WAY_OUT_OF_CONFIRMATION`, `decisionInFlightMessage`, `decisionRecordedTitle`, `decisionRecordedMessage`, `DECISION_REFUSED_TITLE`, `ALREADY_DECIDED_MESSAGE`, `REQUEST_NO_LONGER_LISTED_MESSAGE`, `decidedStateMessage`, `noteStepTitleFor`, `NOTE_STEP_DESCRIPTION`, `REJECTION_NOTE_LABEL`, `REJECTION_NOTE_HINT`, `CONTINUE_FROM_NOTE_LABEL`, `continueFromNoteName`, `WAY_OUT_OF_NOTE_STEP` | `web/src/lib/transactions/deciding.ts` | Deciding one expense request as the user meets it: when the two decisions are offered at all, and every word said about them — stated once for the surfaces that must agree (the row's own controls, the opened request, the note step, and the list that owns the confirmation and the notification). `awaitsDecision` is the ONLY condition on the offer (`Status === 'Imported'`): there is deliberately no amount threshold and no ownership check, and `TransactionRead` carries no subject field to check one against. A rejection's four phrases are all different — "Reject" ASKS, "Continue" moves on from the note, "Reject request" DOES it, "Cancel" backs out — so no query and no user can confuse them, and every control's accessible name carries the request's reference. `decidedStateMessage` is what an opened request says where its decide controls would be once it has been decided — worded from the request's OWN `Status`, so an approved one and a rejected one read differently, and a status this app has never heard of is carried through in the service's words rather than lower-cased into a sentence that would misreport it. `ALREADY_DECIDED_MESSAGE` is the refusal a decision meets when the read taken before submitting shows somebody got there first, and `REQUEST_NO_LONGER_LISTED_MESSAGE` when that read no longer holds the request at all; both are raised on the strength of that read and never of a decide answer, which cannot carry either. Nothing here sends anything — that is `lib/api/decisions.ts`. |
| `SELECT_EVERYTHING_LISTED_LABEL`, `selectRequestLabel`, `SELECTION_COUNT_LABEL`, `AMBIENT_COUNT_CEILING`, `ambientSelectionCount`, `selectionCountMessage`, `NOTHING_SELECTED`, `selectableIdsIn`, `withSelectionToggled`, `withIdsSelected`, `withDecidedRequestsDropped` | `web/src/lib/transactions/selecting.ts` | Selecting several expense requests to act on at once: every word said about a selection, and the set operations that hold one. A selection is a set of transaction IDS, never row positions — that is what lets a tick follow the REQUEST through the list's narrow → order → slice pipeline, and the accepted consequence is that an action may cover requests not on screen. Eligibility is `awaitsDecision` from `deciding.ts`, imported rather than restated, so "which requests can be acted on" cannot mean two things on one screen. `ambientSelectionCount` truncates at `99+` and is for the AMBIENT indicator alone: anything gating an irreversible action states the literal count. Each control names the request it selects (`selectRequestLabel`), because every listed request carries one. `withIdsSelected` ADDS rather than replaces, so taking everything currently listed cannot drop a request selected before the list was narrowed. `withDecidedRequestsDropped` is the other direction: a request that has stopped awaiting a decision — this user's own action or a colleague's, or one that has left the list entirely — comes out of the selection so the visible count corrects itself, and the SAME set comes back when nothing changed, so a read that decided nothing costs no re-render. |
| `ControlTotals`, `controlTotalsOf`, `newestTransactionDateIn`, `batchNameOf`, `runDateOf`, `SelectionSubtotal`, `selectionSubtotalOf`, `figureText`, `PreCommitReading`, `decisionsAwaitingConfirmationIn`, `preCommitReadingOf`, `WHOLE_QUEUE_BATCH`, `NO_RUN_DATE`, `BATCH_LABEL`, `RUN_DATE_LABEL`, `RECORDS_LABEL`, `AWAITING_DECISION_LABEL`, `DECIDED_LABEL`, `TOTAL_VALUE_LABEL`, `WHOLE_BATCH_RECORDS_LABEL`, `SELECTED_VALUE_LABEL`, `NOT_YET_CONFIRMED` | `web/src/lib/transactions/controlTotals.ts` | What a batch of expense requests adds up to, derived in the browser over the ONE fetched set (no endpoint aggregates anything) plus the word each figure is labelled with. `controlTotalsOf` counts the records, what still awaits a decision (`awaitsDecision`, imported not restated) and their summed `Amount`, and takes DECIDED as the REMAINDER — so awaiting + decided always equals records, whatever status vocabulary the service uses. ⚠ `countRequests()` in `src/mocks/data/transaction.ts` is NOT this: its `approved + rejected` counts a row in an unrecognised status as neither, which would ship a wrong outstanding count. Called over the whole set for the batch's own figures and over the narrowed set for the narrowed ones — same function, so the two can never be derived differently. `figureText` states once that a control total prints exactly as the rows print it (no grouping, no currency, no padding — `display.ts`'s "a formatter would be the bug"). `batchNameOf` / `runDateOf` are the whole-queue reading the user settled on where the wire names no batch: `ALL FILES` and the newest `TransactionDate` in the set, sharpening to one file's name and that file's own newest date once the originating-file filter narrows to it — the FILE narrowing only, never a search. `newestTransactionDateIn` compares the service's value as text (in `YYYY-MM-DD HH:MM:SS` that is chronological) and normalises nothing. `selectionSubtotalOf` takes its COUNT from the selection itself so the band and the ambient indicator can never state two figures for one selection, and sums the value over the requests the batch still holds. Also states what the block says while decisions are AWAITING CONFIRMATION (`preCommitReadingOf`, over a set of ids the screen is still asking about): ⚠ only the outstanding count moves — `RECORDS` and `DECIDED` go on stating what the batch IS, so the three visibly do not add up and the reader can see the after-picture has not happened yet; moving `DECIDED` with it re-balances the block and leaves nothing to see. The gap is counted over the set the block is describing and only over requests still awaiting a decision, so what is named and what visibly moved are one number. `NOT_YET_CONFIRMED` is that gap's one phrase, used twice — the block's own label (absent while nothing is pending, never a fixture reading `0`) and the mark on every affected row (`NotYetConfirmedMark`) — so the band and the rows cannot say two different things about one pending decision. Nothing here is optimistic: it is arithmetic over an ask, which is what makes backing out an exact revert with no state to unwind. |
| `BULK_APPROVE_CONCURRENCY`, `DecisionStaleness`, `stalenessIn`, `staleDecisionMessage`, `BulkEligibility`, `eligibilityIn`, `approvedIn`, `ApprovalAttempt`, `submitApprovals`, `BULK_APPROVE_ACTION_LABEL`, `bulkApproveConfirmationTitle`, `BULK_APPROVE_CONFIRMATION_MESSAGE`, `BULK_APPROVE_CONFIRM_LABEL`, `bulkApprovalInFlightMessage`, `BulkApprovalTally`, `bulkApprovalOutcomeTitle`, `bulkApprovalOutcomeMessage`, `retryRefusedApprovalsLabel`, `BULK_APPROVE_REFUSED_TITLE`, `NOTHING_SENT_MESSAGE`, `OUTCOME_UNKNOWN_MESSAGE` | `web/src/lib/transactions/bulkApproval.ts` | Deciding SEVERAL expense requests in one action, and everything said about it. Every part exists because the service has no bulk call and answers the same envelope whether it approved a request or found it already decided. `stalenessIn` is the re-read-before-submit check — is this request STILL awaiting a decision in a read taken now — and it is the ONE such check in the project: the single decision asks it too (`staleDecisionMessage` picks between `deciding.ts`'s two sentences), so "already decided" cannot mean two things on one screen. `eligibilityIn` applies it to a whole selection, and a request it drops is never sent at all. `submitApprovals` is the batch: one call per request, a fixed pool of workers started synchronously so the bound is genuinely in flight at once, never more workers than work, and one refusal never stops the rest — what happened to each request comes back in the order given, for the caller to reconcile. `approvedIn` is that reconciliation: of the requests actually SUBMITTED, the ones a later read shows now carrying a decision — never the call answers, which cannot carry it, and never a request that has vanished from the list, which is an approval nothing can confirm. The confirmation states the selection's LITERAL count however large (the `99+` form is the ambient indicator's alone) and the outcome opens by naming the selection before splitting it into approved / left unchanged / could not be submitted — a bucket with nothing in it is still named, and it leads with words rather than a digit because a notification's heading and its message are read as one string. The third bucket is a call that FAILED and is never the second one: left unchanged is nothing going wrong, could-not-be-submitted is, and it is the only bucket `retryRefusedApprovalsLabel` offers to run again — worded to NAME that subset, both because the retry is scoped and because the list already carries a bare `Try again` on its failed-load state. A retry re-runs the whole of `eligibilityIn` over that subset rather than resubmitting the ids, so a request decided in the interim comes back as left unchanged. Neither surface prints an account number: both are listings (POPIA). |
| `LIST_REFRESH_INTERVAL_MS`, `RefreshedList`, `refreshedList`, `listRefreshedMessage`, `FAILED_REFRESHES_BEFORE_STALE`, `CANNOT_REFRESH_MESSAGE`, `LAST_UP_TO_DATE_LEAD`, `REFRESH_RESUMES_MESSAGE` | `web/src/lib/transactions/refreshing.ts` | Keeping a list current while somebody is reading it, where the service offers only the same whole-set read again: the cadence (15s, stated once, never injectable or shortened for a test), the merge, and the one sentence said about it. `refreshedList` merges a fresh read OVER the one on screen — every request it describes exactly as it already stood keeps the OBJECT the previous read produced, and a read that changed nothing returns the previous ARRAY, so the memoised rows hold, the narrowing/ordering/paging/duplicate memos are not recomputed and a poll that found nothing costs no render at all. Fields are compared over the properties the service actually sent, not a list of names spelled out here, so an optional or newly added field cannot be silently exempt. It also counts how much moved (arrived, changed or left), which is what the polite announcement names — as a RUNNING TOTAL since the reader opened the screen, because a live region announces its contents CHANGING and two identical sentences in a row would leave the second unspoken. Also states when a list has stopped keeping itself current and what it says then: TWO failed reads in a row, counted since the last SUCCESS so one that works in between puts the count back to nothing (one hiccup raises nothing), the plainly worded situation with no technical cause in it, the phrase leading into the moment the list was last current, and that recovery needs nothing from the reader. The moment named is always the last read that SUCCEEDED — never the failure, which would tell the reader the opposite of the truth. |
| `POSSIBLE_DUPLICATE_MARK`, `possibleDuplicateIdsIn` | `web/src/lib/transactions/duplicates.ts` | Which expense requests repeat one another, derived in the browser over the ONE fetched set (no endpoint answers it): the key is `AccountNumber` + `Amount` + `TransactionDate` exactly as the service wrote them (nothing trimmed, rounded or reformatted — `TransactionDate`'s format is an unverified assumption), BOTH members of a match are returned, and REJECTED requests are outside the comparison set — never marked, and never a reason to mark the request they match, because re-submitting something refused is the normal correction. Answers ids, not requests, so the flag rides alongside the narrow → order → slice pipeline without reshaping it; the comparison must never be run over the requests currently on screen, or two matches on different pages would each look unique. Cancelled files' rows are deliberately NOT excluded here — the epic assumes the service never returns them (manual-test ledger), and a speculative `FileLogId` exclusion would bury that assumption. Also states the mark's wording once, for the row, the narrow line-group and the notification. |
| `PossibleDuplicateMark` | `web/src/components/requests/PossibleDuplicateMark.tsx` | The mark a request carries when another request in the same load repeats its account number, amount and transaction date: the shared `StatusBadge` on the `attention` (`--warning`) intent, so it is WORDING paired with a shape and a colour, readable in the list without opening anything and in the same notation as the status beside it. One implementation for both presentations (table row and phone-width line-group), and exactly one element carrying the phrase — no screen-reader-only second copy. |
| `NotYetConfirmedMark` | `web/src/components/requests/NotYetConfirmedMark.tsx` | The mark a request carries while a decision on it is waiting to be confirmed — the reader's own, single or part of a selection: the shared `StatusBadge` on the `attention` intent, carrying `controlTotals.ts`'s `NOT_YET_CONFIRMED` phrase, so it is WORDS beside the status rather than a shape in the gutter (which on these rows is usually already holding the tick that put the decision in flight, and which alone would not satisfy R3). It is the half of the pre-commit state a confirmation dialog cannot satisfy: a dialog DESCRIBES an outcome, the row shows it. One implementation for both presentations, and what tells it apart from `PossibleDuplicateMark` — same intent — is its wording, never its colour. It marks and decides nothing; the list renders it only for the requests a confirmation it opened is still standing over, so backing out takes every one of them off. |
| `EXPORT_COLUMNS`, `EXPORT_MEDIA_TYPE`, `exportHeaderRecord`, `exportRecordFor`, `buildRequestExportCsv`, `expenseRequestExportFileName` | `web/src/lib/transactions/exportCsv.ts` | The hand-over file the external payment system reads, stated once: the NINE RPT-01 columns and their fixed order, from which both the header row and every record are generated, so the two cannot drift. This is the ONE place in the app that writes an account number WHOLE and a transaction type RAW — the documented compliance exception (POPIA masking applies everywhere else), so `lastFourDigitsOf` / `transactionTypeLabel` must never be used here. Values go in exactly as the service sent them: the amount as a bare number (a formatted one is not a number to the receiving system), the date unnormalised, an absent `UserNote` as an EMPTY cell rather than a missing field that would shorten the record to eight columns. Escaping is minimal RFC 4180 — a comma, a double quote or a line break forces quoting, embedded quotes double, and a `\r\n` a person typed survives as `\r\n`. `buildRequestExportCsv` is async because it writes rows a chunk at a time and yields between chunks: at the 10,000-row ceiling one pass would hold the main thread, which is the freeze the feature NFR forbids. No byte-order mark. The file name carries what it holds plus the day and the time of day from the READER's own clock, separated with DASHES — a colon is illegal in a Windows file name and the browser rewrites it silently, so the user would get a name the app never chose. |
| `ExportRequestsAction`, `EXPORT_ACTION_LABEL` | `web/src/components/requests/ExportRequestsAction.tsx` | The control that hands the listed expense requests over as a CSV file (`{ listedRequests: TransactionRead[]; exportedBy?: string }`): it exports EXACTLY the array it is given — the caller's ordered, narrowed set — and narrows, orders or slices nothing itself. Carries NO role check of any kind, and that is structural: both roles may export whatever each has listed, so there is nothing on this surface that can differ and the `hasRole(session, …)` shape submitting a file needs must not be copied here. The file is built ON ACTIVATION, never in a memo — the list re-renders per keystroke and holds up to 10,000 requests — and reaches the user through `deliverFile`, there being no export endpoint to link to. Never disabled: not while a file is being built, and not when the narrowing has left nothing listed, because disabling the control a keyboard user just activated takes the focus out from under them. Says what it produced through the root layout's toast — AFTER the hand-over, so it confirms a completion and not a click: how many requests went into the file (the count of that array, and nothing beside it — naming the unfiltered total as well invites the misreading BR1 exists to prevent), plus who produced it and when (`onScreenDateTime`). That attribution is the SECOND HALF of the epic's compliance exception, the file carrying account numbers whole, so it is evidence rather than polish and may never be dropped. `exportedBy` is the signed-in person's name from the server page (`displayNameOf(session)`) — nothing here reads an identity in the browser — and is optional for the same reason `actingUploader` is: a render with no session behind it names only the moment. A narrowing that has hidden every request produces NO file (not an empty one, not a header row alone) and says so in `NARROWED_EMPTY_MESSAGE`, the sentence the rows' place already uses. One moment is read at activation and used for both the file name and the confirmation, so the two cannot name different minutes. |
| `SORT_STORAGE_KEY`, `rememberedSort`, `rememberedSortOnServer`, `subscribeToSort`, `rememberSort` | `web/src/lib/transactions/sortPreference.ts` | The ordering the reader chose for the request list, remembered in `sessionStorage` for the rest of the session and exposed as external state React watches (`useSyncExternalStore`) — so it survives the component unmounting on a client-side navigation and mounting again on return, without being copied into component state by an effect. The server snapshot is the honest "nothing chosen". A stored value is validated against `REQUEST_COLUMNS` before it is trusted, and unavailable or unrecognised storage simply means no ordering was chosen. |
| `BatchControlBlock`, `CONTROL_BLOCK_LABEL` | `web/src/components/requests/BatchControlBlock.tsx` | The request list's control block: the batch's record count, what still awaits a decision, what has been decided, its total value, which files it covers and its run date — the band that REPLACED that screen's page title, so the screen opens with where the batch stands rather than with its own name. One named `region` holding one label-over-figure pair per figure; each figure's visible tracked label IS its accessible name (`aria-labelledby`), and the named element carries the figure and nothing else, so a screen-reader user hears "Awaiting decision, 5". Derives nothing itself — it is handed the whole fetched set, the narrowed set the list's pipeline already holds and the selection, and reads `controlTotals.ts`. While a narrowing is active the four totals describe what is LEFT and the whole-batch record count is kept beside them struck through, named for what it is (a line through a number is silent to a screen reader) and ABSENT from the markup otherwise; the selection's count and value join the row only while something is selected, absent rather than a permanent "0". The outstanding count is the single largest element on the screen (~8:1 against its 11px label, no middle tier between them) on the saturated `--brand-accent` field, full-bleed by cancelling the layout's own `px-4`/`py-8` and re-applying its padding — change either layout value and this changes with it. The band REFLOWS at phone width (its fields wrap onto further lines) rather than scrolling sideways, and the outstanding count is still the largest text painted anywhere on the screen there. Each field's text closes with one space, so the band's own text run reads as separate fields rather than one welded token (`RECORDS 8 AWAITING DECISION 5`); a white-space-only run inside a flex or grid container is not rendered, so it costs no layout. It is also where the batch's pre-commit reading is shown: handed the ids a decision is awaiting confirmation on, it states the PROJECTED outstanding count beside an unmoved `RECORDS` and `DECIDED` and adds the `NOT YET CONFIRMED` pair naming the gap (both from `controlTotals.ts`, which owns the ⚠ about which figure may move) — absent while nothing is pending, and reverted by the set simply emptying. And it carries the screen's ONE orchestrated motion: the outstanding count settling onto its new value through the `figureRoll` keyframes, animated on a `block` child of the figure so a transform moves the digits while the figure's own box — and everything below the band — stays exactly where it is. The roll follows the SETTLED count, never the projected one, so committing a decision settles once instead of three times (into the projection, back out, then down), and it is applied only to a figure that has something to roll to, never on first paint. |
| `RequestListPagination` | `web/src/components/requests/RequestListPagination.tsx` | The foot of the request listing, as its CONTINUATION LINE: `RECORDS 1–20 OF 428 · PAGE 1 OF 22` in the shared tracked micro-label notation, tabular so the line holds its width as the reader pages — ONE element whose entire text is the line, and the screen's ONLY statement of the records range and the page counter (a second copy anywhere, including a narrow-width or screen-reader-only one, leaves the reader two answers to "where am I"). Its figures come from `recordRangeOf` beside the slice the rows are drawn from, so the line cannot describe a page the listing is not showing; a page holding one record collapses to `RECORDS 1 OF 1`, never `RECORDS 1–1 OF 1`. It draws NO rule of its own — the listing's own closing hairline is the single rule between the last row and the line, and is therefore the line's top edge too. Beside it: the page-size selector as a ruled FIELD (`RULED_FIELD_CLASS` plus its micro-label — a styling decision only; still a Shadcn `select` named for the field through its own `Label`, so its name is not the size it displays, offering `PAGINATION.PAGE_SIZE_OPTIONS` and never re-declaring them) and previous/next inside Shadcn's `pagination` landmark, wearing the same ruled action notation as every other non-row control on the screen. Previous/next are real `<button>`s carrying `disabled` — the primitive's own previous/next slots are anchors, which cannot be disabled and stay in the tab order. The primitive's `PaginationContent`/`PaginationItem` (`ul`/`li`) slots are deliberately NOT used: at phone width the listed requests are the screen's only list, one `listitem` per request, and page controls must not add more. Nothing is ever removed when the set fits one page; it is disabled instead. Nothing here is a live region: the rows are the answer to a page change, and the page count moves on every keystroke in the search box. |
| `StatusBadge`, `StatusMark`, `statusInkFor`, `StatusIntent`, `StatusPresentation`, `STATUS_INTENTS` | `web/src/components/status/StatusBadge.tsx` | The one way a status is shown: a RULED MARK — a drawn shape beside the status in the shared tracked-label notation (`FIELD_LABEL_CLASS`), in the intent's ink — never colour alone, and never a pill (no radius, no background, no border, and deliberately NOT the Shadcn `badge` primitive, whose unconditional `rounded-full` capsule is the pill this replaced; that primitive is gone from the project). Callers name an INTENT (`informational` / `positive` / `attention` / `negative` / `neutral`) and hand over the raw status string; the intents, their tokens AND the shape each is drawn as all live here, so two surfaces can never draw the same state two ways. The shapes differ in FORM, not tint, so they read in greyscale: hollow rule-box (informational/awaiting), the box inked solid (positive), the box struck through (negative), a rule and no box (neutral — also the CANCELLED shape, which must render correctly though no listed request can be cancelled), a doubled margin bar (attention). `StatusMark` is that shape on its own, `aria-hidden` and sized by the caller, for a surface that shows the shape where the word is read elsewhere on the row (a listing's reserved gutter) — meaning is always carried in words as well; `statusInkFor` answers that intent's ink as a token class, for a mark rendered alone inside a row that has receded, where `currentColor` would mute the one thing still marking it. The intent → token mapping stays stated only here. Omitting the presentation is how an unrecognised value is shown — neutral, SHAPELESS (a shape would claim a meaning the app does not have), in the service's own words. Consumed by the file-status vocabulary, the import preview's two verdicts and the expense request list. Also carries marks that are not statuses but read the same way — a short phrase paired with a shape and an intent colour (see `PossibleDuplicateMark`); anything of that shape belongs here rather than in a mark of its own. |
| `ExpenseRequestList`, `NARROWED_EMPTY_MESSAGE` (re-export) | `web/src/components/requests/ExpenseRequestList.tsx` | The shared expense request list (two optional props, reads itself from the browser): the whole fetched set held in memory — narrowing, sorting and paging compose over it in ONE pipeline (narrow → order → slice) and never re-read, so what is ordered and paged is what the narrowing left rather than the whole fetched set — one row per request carrying the service's own values, the plain-language transaction type, a masked account number (last four digits only; the full value is reachable only inside one opened request, never from the list), and the status badge. Which presentation the reader gets is decided by the watched viewport width — the table at `md` and above, `RequestCards`’ ruled line-groups below it — and only ONE of the two is ever in the markup. Holds WHICH request is open, by id, and renders `RequestDetailPanel` for it; opening one changes no narrowing, ordering or page, so closing it returns the reader to their place. Rows and narrow line-groups are memoised on stable props (the request, plus one callback that takes it), so a keystroke that leaves the page unchanged re-renders neither. The narrowed set is ordered once per change and a page is a slice of that array, never re-derived per row. Every control that changes the set underneath returns the reader to the first page, and the page shown is clamped to a real one so a set shrinking from elsewhere cannot strand them past the end. Answers the tiered wait (nothing under 300ms, announced placeholder from 300ms, still-loading message joining it past 3s), the nothing-ever-imported state offering the upload action, and a failed read carrying the service's wording plus Try again. Owns the narrowing state (component state, never the URL — the endpoint takes no parameters) and the narrowed-empty state, which names what is applied and deliberately offers NO upload action, that being the whole distinction from the nothing-ever-imported one. Hands a range entered the wrong way round to the strip to announce in place (`rangeReports`, off the same deferred narrowing the rows and the summary read) and applies neither of its bounds, so the list is left as it was rather than going unexplainedly empty. Its own first line — the export, and the Approver's selection controls and bulk action — sits ON the strip in the strip's notation, full-bleed and ruled like it, because those are what the reader may do to the set the strip decides. Marks possible duplicates from `lib/transactions/duplicates.ts` — derived in a memo over the FETCHED set (never an effect, which would re-derive per render) and passed to each row/line-group as a boolean, so the marks survive narrowing, ordering and paging — and notifies the Approver ONCE per load when a load marks anything, through the root layout's toast, from the optional `roles` prop the server page fills from `rolesOf(session)`. That prop exists for that one decision: the list is identical for both roles, a render without it notifies nobody, and which duplicates have been announced is held per request id in a ref, so a dismissed notification stays dismissed through every later re-render. Offers the CSV export (`ExportRequestsAction`) the ORDERED, NARROWED array the pipeline already holds — never the page on screen, which would ship a one-page file that looks perfectly correct, and never the fetched set, which would ignore the narrowing — plus the `exportedBy` name the server page fills from `displayNameOf(session)`, passed straight through so the export's attribution is the identity the server gated on. Also owns the DECIDE flow the row, the line-group and the opened request only ask for: it hands `onDecide` to each of them solely for an Approver (the same `roles` prop, no second gate) looking at a request `awaitsDecision` still allows, holds which decision is pending, and renders the shared `ConfirmAction` for it. A rejection is asked WHY first: choosing Reject opens `RejectionNoteStep` instead of the confirmation, and the note it hands back travels with the decision as `UserNote`; approving asks for nothing. So nothing is sent until the confirmation is accepted, and the confirmation closes as the decision is submitted whichever way it turns out. The outcome then arrives by RE-READING (`GET /v1/transactions`), never out of the decide answer, which carries no status; that re-read is what moves the status, withdraws the decide actions and updates the open panel, and one that fails leaves the last known rows on screen. The same read is taken BEFORE anything is sent, which is what holds one decision per request: an accepted confirmation re-reads the list first and submits only while that fresh read still shows the request awaiting a decision — otherwise NO decide call is made at all and the refusal is raised locally in the brief's words, the same read having already put the decision that was actually recorded on screen. A read that fails also sends nothing: a decision goes out only while the app can see the request is still open to one. Each decision on its way is announced per request id, so two in flight cannot clear each other's line. A recorded decision confirms itself on the toast's default duration and fades; one that was refused is raised with `duration: 0` and carries the service's own wording. Also holds WHICH requests are SELECTED to be acted on together — a set of ids kept ABOVE the pipeline, so a tick survives a search that hides the request, an ordering that moves it and a page nobody is reading, and the count never changes with the view. Every row and line-group is handed a plain `selected` boolean and one callback that takes the request: handing the set down would defeat their memo on every tick and on every refresh. All of it — the per-request tick in the gutter, the select-everything-listed control and the ambient count — is offered to an Approver alone and absent from everyone else's markup, never disabled. "Select everything currently listed" covers the NARROWED set (not the page on screen, not the fetched set) and only requests still awaiting a decision; unticking it is the screen's clear-the-selection action, and it clears the whole selection including whatever the current narrowing hides. The count is on screen only while something is selected — no permanent "0 selected" — and is the one place the `99+` truncation applies. Beside the count sits the action it leads to: approving the whole selection, through the SAME shared `ConfirmAction` one request uses, then a fresh read that drops anything already decided, one call per remaining request at `lib/transactions/bulkApproval.ts`'s bound, and a read BACK whose comparison with the first is where the approved count comes from — the answers cannot carry it. The selection controls and that action are DISABLED while the batch runs, which is the one place this screen disables rather than omits: transient state, not a permission. The list stays readable throughout and the batch announces itself on the same polite line an in-flight single decision uses, since no row changes until the read lands. A batch whose calls partly FAILED reports that as a third bucket on a notification that does not fade and carries the toast surface's `action` button, which re-runs the SAME batch over exactly those requests — no second confirmation (this one was confirmed when it started), no second batch path, and the guard on "a batch is already running" is a ref rather than state, because that control is created while the first batch is still in flight. Finally, it KEEPS ITSELF CURRENT: the same read again on `refreshing.ts`'s interval, for as long as the screen is open and the document is visible (`lib/layout/pageVisibility.ts`), paused while the reader's own bulk confirmation is open or its batch is in flight and resumed the moment that ends — backing out included — which is also what stops a poll firing a second whole-list read beside the batch's own. A reader returning to the tab is read for STRAIGHT AWAY rather than waiting out the tick they missed. Every read after the first goes through `refreshedList`, so a refresh updates in place: no dialog closes, no focus moves, no narrowing, ordering or page changes, and only the requests that moved re-render. The one thing it deliberately changes is the selection (`withDecidedRequestsDropped`), so a request a colleague decided leaves it and the count corrects itself with nothing raised. What arrived is said on ONE `aria-live="polite"` line, present from the start and empty until there is something to say — `aria-live` rather than `role="status"` because this screen is asserted to be announcing NOTHING while it waits, while it is empty and once it has simply answered. And when that refresh stops working it admits it: after `refreshing.ts`'s two consecutive failures, ONE polite `role="status"` notice states the situation and carries a machine-readable `<time>` holding the instant of the last read that succeeded — held in refs (the instant, and the failure count) so a poll that simply worked still costs no render. Never the failed-load `Alert`, which belongs to a read that left the reader with nothing: here every row, the narrowing, the page and the keyboard stay exactly as they were. The notice carries no button and no link at all — the timer never stopped, so the next read that lands clears it with nothing pressed, typed or reloaded. The desktop listing is RULED, not framed: the Shadcn `table` primitive restyled through — real `<table>` semantics, `<th scope="col">` and `aria-sort` intact — inside a full-bleed box (`-mx-4`, cancelling `<main>`'s `px-4`) so every hairline row rule reaches the edge of the page like the strip's rule above it, with the page padding applied to the FIRST and LAST cell instead of to a box around the rows, which is what keeps the values lined up with the control block's labels; the closing hairline is on that box, the primitive leaving the last row unruled. Its FIRST column is the RESERVED EXCEPTION GUTTER: two characters wide (`w-[2ch]`, measured in the listing's own mono face so "two characters" is literal), on every row and for every reader, never dropped or collapsed when nothing on the page needs marking — that empty column is what makes a marked row findable by scanning one column — and headed `sr-only` "Exceptions and selection", a name deliberately carrying no word another heading carries, since every surface here addresses a column by its own distinctive word. Two characters hold ONE mark: an Approver's selection tick on a request still awaiting a decision (the bolted-on checkbox column is GONE, not hidden; the tick is still the Shadcn `checkbox` — a real control with its own checked state, named for its request, disabled only while a batch runs — restyled square and unshadowed, so it reads as the taxonomy's hollow box unticked and its inked box ticked), else the shared `StatusMark` for a decision already recorded, in that intent's own ink through `statusInkFor`. An ordinary undecided row's gutter carries NOTHING — no placeholder glyph, no dash. A row needing attention is marked by a RULE down the cell's outer edge rather than by a shape, because those two characters may already be carrying the offer to select it, and a possible duplicate still awaiting a decision is the row an Approver most needs to find; its wording stays beside the status. A request that has stopped awaiting a decision (the same "not awaiting" remainder the control totals take) drops to `--muted-foreground` so only rows still awaiting one hold full contrast — still listed, still readable, controls still working, never `aria-hidden`, `aria-disabled` or dimmed, and the decision itself keeps full ink in the gutter and in the status beside it. There is no card, no panel and no striped-row treatment anywhere on it, and the primitive's per-row hover fill and transition are cancelled at the row — a row that tints under the pointer is the stripe arriving one row at a time, and the render budget at the 10,000-request volume has no per-row effects in it. Column heads are the shared tracked micro-label notation (`FIELD_LABEL_CLASS`) in `--muted-foreground`, capitals by `text-transform` so the sort control's accessible name is unchanged, with the sort glyph sized down to the label. Every figure is right-aligned, mono and tabular; the reference, the transaction date and the masked account number are mono set ON THE CELL, never on the shared masking component, which prints a screen this epic does not restyle. All four non-row answers — the tiered wait's placeholder, the never-imported state, a failed read and a narrowing that hid everything — plus the stopped-refreshing notice are composed as the SAME full-bleed band closed by a hairline top and bottom, in the rows' own rule weight: with the card gone there is nothing else framing an answer, and each keeps its existing wording, role and actions exactly (the failed read's `alert` is stripped of the primitive's own card and sits inside the band). Its two non-row controls (the empty state's next step, the retry) wear the strip's ruled action notation, no boxed buttons left on the screen. And it OPENS with the batch's control block (`BatchControlBlock`), which replaced this screen's page title: the block is handed the fetched set, the narrowed set the pipeline already holds and the selection, so every figure is a view over the same arrays the rows are drawn from and nothing is fetched, counted or narrowed a second time for it. It reads the DEFERRED narrowing, like the rows and the summary do, so the band and the listing move together; and it is on screen only once there IS a batch to describe — the tiered wait, a failed read and the never-imported state keep their own answers, unaccompanied by a band of zeroes. Finally, before an irreversible decision commits it shows the batch AS IT WILL BE: one derived set of ids (the single pending decision's request, else the whole selection while the bulk confirmation stands — gated on `bulkApprovalAsked`, so a refused request left selected for a retry claims nothing) drives both readings at once, the block's unbalanced figures and the `NotYetConfirmedMark` on every affected row and line-group. DERIVED, never stored and never optimistic: the machinery underneath is untouched and still learns what happened by re-reading, so backing out of either confirmation restores every figure and every mark by construction, with no second state to unwind. The rejection note step is deliberately outside it — nothing has been asked to be confirmed yet. |
| `RequestNarrowingControls`, `NARROWING_STRIP_LABEL` | `web/src/components/requests/RequestNarrowingControls.tsx` | The list's narrowing controls, as ONE RULED STRIP: a labelled search box, the pick-one filters (one `PickOneFilter` implementation for each, so the label wiring and the reset choice cannot drift) and the two-bound ranges' four bound fields (one `BoundInput` implementation each, `type="number"` for an amount and `type="date"` for a day). All eight sit inside exactly ONE `<fieldset>` named natively by its `<legend>` — "these fields are one thing" is only observable outside a browser in the accessibility tree, so a second group over a subset, or a field that escapes the fieldset, breaks the strip even while every control still works. Each field wears the shared `RULED_FIELD_CLASS` — an UNDERLINE and nothing else, imported rather than restated because the foot's page-size field wears the same one — which is why the primitives' `border-input` COLOUR is never overridden — that underline is now the only thing outlining the field, and `--border` in its place would drop it below 3:1 (WCAG 1.4.11). Labels are the shared tracked micro-label notation, upper-cased in CSS. Takes the WHOLE fetched set and offers each filter's choices from it (never from what is currently listed, which would leave a filter unable to offer its way back). Every bound is a TYPEABLE field — a calendar popover may sit beside one, never instead of it, or the keyboard-completability bar cannot be evidenced — and holds exactly what was typed, leaving what counts as a bound to the narrowing layer. It also REPORTS a range entered the wrong way round, in place: the `rangeReports` it is handed (the caller's, off the same narrowing the rows read, so the strip cannot report a range the list is quietly applying) become a `role="alert"` line per range inside the strip, and the two bounds of that range carry the error in the notation instead — `aria-invalid`, a destructive underline at double weight, and `aria-describedby` pointing at the message. Never a box around it and never a toast, which would take the report away from the fields it is about. Where a further filter kind belongs. |
| `FIELD_LABEL_CLASS`, `RULED_FIELD_CLASS` | `web/src/components/requests/fieldNotation.ts` | `RULED_FIELD_CLASS` is the app's ONE ruled field notation — a field marked by an UNDERLINE and nothing else (`rounded-none border-0 border-b bg-transparent px-0 shadow-none`, plus the dark-mode surface cancels), carrying NO underline colour so the primitives' own `border-input` stands (`--input` is the darker hairline token precisely so an underline alone clears 3:1, WCAG 1.4.11) and leaving the focus ring untouched. Size and width belong to the surface: the narrowing strip's eight fields fill their columns, the foot's requests-per-page field is as wide as the figure it holds. `FIELD_LABEL_CLASS` is the app's ONE tracked micro-label notation — 11px Azeret Mono, tracked, upper-cased in CSS (never in the DOM, so the wording a screen reader is given still reads as words). Every label on the request list is the same object, so it is one string: the control block's figure labels, the narrowing strip's micro-labels, the actions sitting with the strip, the summary of what is applied, the listing's own column heads and every control a row offers all import it — as does the shared status mark (`StatusBadge`), which reaches every screen showing a status, not just this one. It deliberately carries NO colour (a label is muted on the ground and the band's own foreground on the accent field, and two `text-*` colours on one element resolve by stylesheet order rather than by the order they are written) and no spacing. |
| `MaskedAccountNumber` | `web/src/components/requests/MaskedAccountNumber.tsx` | The only way an account number is printed anywhere rows are listed (imported requests, and a failed file's rejected rows): its last four digits (from `lastFourDigitsOf`), the dots beside them `aria-hidden` and replaced for a screen reader by "ending in", and "Not available" when the service sent no digits at all. The full value is never rendered, and never put in a `title` or `data-` attribute — a value parked in an attribute leaks exactly as surely as one printed in a cell (POPIA). It carries no TYPEFACE of its own, deliberately: the surface printing it sets the face (the request list's cell is mono, a rejected-rows table is not), so restyling one listing cannot reach a screen the same masking serves. |
| `RequestActions` | `web/src/components/requests/RequestActions.tsx` | What a reader may DO with one expense request, for both presentations: open it, and — given `onDecide` — approve or reject it. EVERY action is a direct control on the request, one activation away; there is no ⋯ overflow menu, so no action is offered in two places and none of these controls is icon-only. Every control's visible wording is the start of an accessible name that goes on to name the request (`decideActionName` / "Open request …"), which one screen holding one Approve per listed request makes load-bearing rather than decorative. `onDecide` arriving is the WHOLE condition on the decide controls: absent, neither is rendered — not a disabled one, not a hidden one — so who may decide and which requests can still be decided stay the list's to answer and can never be re-derived here. Withdrawing them HANDS THE KEYBOARD to this request's own Open control (`handOffFocus` from the caller, reported back through `onFocusHandedOff`): the hand-off fires on the commit that removes the decide controls — never earlier, because the confirmation's own deferred focus restore would undo it — and only when focus is still on this request's controls or has already been dropped. All three controls wear the SAME ruled notation as the strip's own actions (`FIELD_LABEL_CLASS` on a rule, glyph sized on the ICON because the primitive's own icon-sizing selector beats a button-level override) — one string for the three, so what tells them apart is the word and the glyph, never weight or colour: a filled control repeated once per row was the loudest thing on a screen whose one saturated field is the control block, and a decision is protected by its confirmation rather than by how heavy its button is. |
| `RequestDetailPanel` | `web/src/components/requests/RequestDetailPanel.tsx` | One expense request opened over the list: every value the service holds, printed as it arrived, in a `dialog` whose accessible name carries the request's reference — no editable field, no form, nothing to submit. The only thing that can be done to the request from here is DECIDE it, and only through `onDecide`, which the list hands over on exactly the same condition as the row's own controls (an Approver, a request still awaiting a decision); absent, the reader is offered nothing rather than a disabled control — and a request that has already been decided states where it stands (`decidedStateMessage`) in that same place, to both roles, since that describes the request rather than what the reader may do to it. Holds the account-number reveal as its OWN state and is mounted only while open, so the reveal cannot follow the reader onto another request, another page, or back onto the same request; that containment is the compliance requirement made true by construction rather than by resetting a flag, so it must never move into a store, a context, the URL or a ref. Escape and a plainly worded Close both close it (the primitive's icon-only corner close is off — an icon-only control would owe a tooltip). |
| `RejectionNoteStep` | `web/src/components/requests/RejectionNoteStep.tsx` | The reason a rejection has to carry, asked for BETWEEN choosing Reject and the confirmation — never inside it, because the confirmation's way out holds focus precisely so a stray Enter decides nothing and an editable field in it would defeat that. A Shadcn `dialog` composing `textarea` + the `form` primitive over `rejectionNoteSchema`, checked ON SUBMIT and re-checked the same way (`mode` and `reValidateMode` both `'onSubmit'`), so nothing is said while the user is still typing. Its send control is NEVER disabled: one that becomes usable only once the note is non-empty can never take keyboard focus while it is empty, leaving the refusal unreachable without a mouse. It records nothing and sends nothing — it hands the trimmed note back, and the caller opens the confirmation with it. |
| `RequestCards` | `web/src/components/requests/RequestCards.tsx` | The expense request listing as a PHONE-WIDTH reader receives it: the same ruled listing the wide screen draws, one group of ruled lines per request inside a `ul role="list"` — full-bleed, each group closed by a hairline and separated by NOTHING else, so consecutive groups touch. The name is historical: the Shadcn `card` per request it once composed is a named anti-goal of this design and is GONE, not extended — neither a group nor anything in it may be a card, and a wide table in a sideways-scrolling wrapper does not satisfy the narrow-viewport requirement either. Exactly ONE `listitem` per request, which at this width is the screen's only list. Carries the reference as its primary identifier, the amount, the status (`StatusBadge`) and the transaction date under the shared labels the wide listing heads those columns with, plus the same `PossibleDuplicateMark` and `NotYetConfirmedMark` the row renders — a mark has to be readable at every width, so a phone-width reader being asked to confirm a decision sees the same pre-commit state — and `RequestActions` on a line of its own, decide controls included, on the same condition the row applies: a phone-width reader is not a read-only reader, and three controls beside the reference at 360px would crowd it. Everything the reader reads sits in one column that starts at the same place in every group, so the marks and the statuses read as columns rather than as ragged text. THE RESERVED GUTTER is here too, two characters and never collapsed, holding at most one mark (the tick, else the shared `StatusMark` for a decision already recorded, inked through `statusInkFor`) with an exception marked as a rule down the group's outer edge instead; the gutter's own type scale is LARGER here, so two characters is about 24px — the target size a finger needs (WCAG 2.2 §2.5.8) with `min-h-6`/`min-w-6` stating that floor explicitly. A decided group recedes to ink-on-ground, the decision keeping full ink. No account number is printed here at all; the reader gets it by opening the request. Which shape a request's state puts in the gutter and how its status reads are handed in, not re-derived, so the two presentations cannot mark the same request two ways. |
| `AppliedNarrowingSummary` | `web/src/components/requests/AppliedNarrowingSummary.tsx` | What is currently narrowing the list: a named `region` holding one list item per active narrowing, from the list handed in rather than a fixed set of chips, beside the screen's only Clear all — which is offered whenever anything is applied, not just once the narrowing has emptied the list. |
| `SubmittedFilesList` | `web/src/components/files/SubmittedFilesList.tsx` | The submitted expense files table (`{ viewerRoles?: string[]; actingUploader?: string }`, reads itself from the browser): one row per file with the service's own values, a status chip that pairs an intent colour with the status text, a per-row link into that file's own page (from `submittedFileAddress`, and the only way that page is reached), and the three states — announced busy (`role="status"`), "nothing submitted yet", and a failed load carrying the service's wording plus Try again. Re-reads itself when a submission is announced and, while any listed file is still in progress, on a 15s timer that stops once nothing is; a re-read that fails leaves the last known rows on screen. Announces both outcomes through the root layout's toast, on the TRANSITION into the status (previous status per file id in a ref, so a file already finished when the screen opened is never announced and one that stays finished is not announced twice): `Imported` as a fading confirmation (file name + the service's `RecordCount`), `Validation failed` as a notification that does NOT fade and carries the link to that file's rejected rows. `viewerRoles` is the watching person's role names, filled by the page from `rolesOf(session)` — nothing here reads an identity in the browser — and it narrows the rejected-rows notification to `ROLE_IMPORTER` only; ABSENT means "the caller did not say", which still notifies, because the component's original contract carries no session prop at all. The import confirmation is not role-gated. Each row also offers `Delete file` IN THE ROW beside the Open link — never behind a per-row menu, which would put a control between a keyboard user and the action — with NO status condition on it, an `Imported` file included. That control is offered only where `actingUploader` is supplied, and its ABSENCE means no delete control at all: the OPPOSITE polarity to `viewerRoles`, deliberately, which is why the two are separate props — a caller that has not said who is acting must not be handed a destructive action the server never authorised. Absent, never disabled. The dialog a row opens is the shared `DeleteFileConfirmation` (one controller for the whole table, since only one row can be being CONFIRMED at a time) handed the rows currently on screen, so a row and the file's own page read identically for the same file and a file that imports under an open confirmation is described by what it is now. A confirmed delete goes through `deleteSubmittedFile` with `actingUploader` as the audit identity and then simply asks for the LIST again through the same read Try again and a submission use — the row disappears because the service was asked, never because a local array was spliced, and no second timer is involved, so the in-progress auto-refresh goes on working afterwards. Only the LAST read asked for may answer (each read carries the number it was issued with), so a timed read that set off before the delete and lands after that re-read cannot put the deleted row back on screen. Delete progress is held PER FILE, never one state for the table: a second row's confirmed delete is issued alongside the first rather than silently dropped, and every in-flight line and every refusal names its own file — the only press ignored is a second one for a file whose own delete is already on its way, whose in-flight line is on screen naming it. A refusal is reported above the table in the SERVICE's own words, naming the file (on a list of many, "this file" would leave the reader guessing which row), with the dialog closed, every row exactly where it was and the action offerable again; nothing navigates, which is the file page's behaviour on success and has no meaning here. When a row this screen deleted has gone AND focus went with it (the dialog returns focus to the control that opened it, which the re-read then unmounts, dropping focus onto `<body>`), focus is moved to the section's own heading — `tabIndex={-1}`, so it is focusable on purpose without joining anyone's tab order — rather than leaving a keyboard reader to start their next Tab at the top of the document. |
| `SubmitExpenseFileForm` | `web/src/components/upload/SubmitExpenseFileForm.tsx` | The expense-file submit surface (no props, reads the settings itself from the browser): a Shadcn `select` of the ACTIVE named settings only, a real `<input type="file">` in the tab order, and one submit that stays unavailable until a setting and a CSV are both chosen. Refuses a non-CSV by name before anything is sent, and says how the submission went in-page as one `role="alert"` — the service's own reason on a refusal, with both choices left in place. A screen renders it only for a session that may submit. |
| `FileStatusBadge` | `web/src/components/files/FileStatusBadge.tsx` | The ONE file-status vocabulary in the project: what each of the five recognised `CurrentStatus` values means, handed to the shared `StatusBadge` (which owns the intents, their tokens, the shape each is drawn as, and the neutral/shapeless treatment of a value this app has never heard of). Every surface showing a file's status uses this rather than mapping the statuses again. |
| `SubmittedFileDetail` | `web/src/components/files/SubmittedFileDetail.tsx` | One submitted file, as the browser resolves it: the whole client surface of `/upload/file`, taking the identifier EXACTLY as it arrived in the address (`{ logId: string \| undefined; actingUploader?: string }`, never narrowed to a number). `actingUploader` is the server's decision about who may act on the file (`actingUploaderIn`, shared with the Expense files list) — one value that both gates the uploader-only actions and supplies the delete's audit identity, so the recorded name can never be something a user typed. While the file is in progress it re-reads its OWN calls on ONE interval (15s) and the history's and rejected rows' with them, via the `refreshSignal` it hands both; a retry it is told about asks the same calls again at once AND keeps that interval running for a fixed number of further reads, because a retry that fails again leaves the file in the status it already had — so the file need never look in-progress and a watch keyed only on that would never start; a re-read that fails leaves the last known values on screen. There is no get-one-file endpoint, so the file is found in the ACTIVE list (`fetchSubmittedFiles`) — which is also what makes a cancelled file answer itself: inactive means absent from that body, so cancelled, unknown and unusable are ONE answer (this file is not available, plus the way back to the list), carrying no service-error wording and no history, because nothing went wrong. A read that FAILED is the separate answer, with the service's own wording and its own way to ask again — worded `Load this file again`, never `Try again`, which the processing history owns on this same screen. Shows the file's own values verbatim (setting, processed time, status chip, record count, most recent activity) and composes the sections other file capabilities render inside. |
| `FileProcessingHistory` | `web/src/components/files/FileProcessingHistory.tsx` | One file's recorded processing activities (`{ logId: number; refreshSignal?: number }`, read from the browser — a changed `refreshSignal` means "ask again", so the caller that knows the file's status drives this list from its own single interval instead of this one growing a timer it cannot decide when to stop): a table with one row per activity carrying its name, the outcome and both times exactly as the service sent them — nothing sorts, reformats or judges them. An activity still running has neither outcome nor end time and is listed anyway, saying nothing has been recorded yet rather than inventing either. Answers the announced wait, the nothing-recorded-yet sentence (an answer, so no alert and nothing to ask again for) and a failed read carrying the service's own wording plus exactly one `Try again`. |
| `RejectedRows` | `web/src/components/files/RejectedRows.tsx` | Which rows of a submitted file validation rejected (`{ file: FileLog; refreshSignal?: number }`, read from the browser — a changed `refreshSignal` means "ask again", so the caller that owns the file page's single interval drives this list too; without it a file that failed validation AGAIN would keep the previous attempt's rows on screen, since nothing about the file changes): renders NOTHING AT ALL — no heading, no table, no read — unless the file's status is `Validation failed`, and takes no session or role prop because both roles read it. One table row per rejected row carrying that row's seven recorded values exactly as the source file held them (an amount that is text and a date that is not a date are printed verbatim; `transactionTypeLabel` is deliberately NOT applied, because the list exists so the user can correct their own file), with the account number masked and a per-row reveal whose revealed set lives in the loaded state — so a re-read starts fully masked by construction, and there is no reveal-all. The defect wording comes from the shared `lib/files/defectWording.ts` (the import preview and its correction CSV must read the same way) rather than being stated here. A row with no defect signal is still listed, saying no reason was given rather than inventing one. Answers the announced wait, the no-rows-reported sentence, a body that cannot be read as rows (says so, offers to ask again — never an empty table and never a throw, which would take the whole file page down) and a failed read carrying the service's own wording; both recovery actions are worded `Try again to load the rejected rows`, never the bare `Try again` the processing history owns on this same screen. |
| `FileDownloadActions` | `web/src/components/files/FileDownloadActions.tsx` | The files a user may take away from one submitted file (`{ file: FileLog }`, no session or role prop — both downloads are offered to both roles, so there is nothing on this surface that can differ): `Download original file` always, and `Download error file` only where the service reported one (`HasBulkErrorFile` is the STRING `'Yes'`, so a truthiness check would offer it for `'No'` too), absent from the markup otherwise rather than disabled. Both are CONTROLS, never an `<a href>` at a download endpoint — a link would drop the user onto a raw error response — so the bytes are fetched from the browser through the shared client and handed over by `deliverFile`, and a refusal is reported here in the service's own words with both controls left usable, which is the section's whole retry affordance (the words `Try again` belong to the processing history on this same screen). Neither control is disabled while its file is on its way: disabling the control a keyboard user just activated takes the focus out from under them, so the wait is announced instead — and because both are therefore usable at once, each download holds its OWN wait and its OWN refusal (keyed by its label), so whichever answers first cannot clear the other's announced wait or overwrite its refusal. An in-flight guard is deliberately NOT used: a control that looks usable must never be a silent no-op. Each control also carries its OWN description saying which file it hands over, tied to it with `aria-describedby` — both of these are the service's own files, which is what tells them apart from the import preview's correction download on the same page; a paragraph merely sitting near two buttons describes neither. |
| `ImportPreview` | `web/src/components/files/ImportPreview.tsx` | Every row of a submitted file and what will happen to it (`{ file: FileLog; refreshSignal?: number }`, read from the browser — the same signal convention its siblings take, so the file page's single interval drives it and it grows no timer): renders NOTHING AT ALL — no heading, no table, no read — until the file's validation has run (`Imported` or `Validation failed`), and takes no session or role prop because both roles see everything here. Two reads through `lib/api/files.ts` and no others (the file's own bytes from `/v1/files/download`, read with `readBlobText` and parsed by `parseSubmittedFileCsv`; the rejected-row overlay from `/v1/files/validation-errors`) — never the service's generated error file, never `/v1/file-logs/data`, and never `GET /v1/transactions`, which the epic demotes to future confirmation of what actually imported. ONE ORDERED LIST, not two stacked halves: the rows sit in the positions their file gave them, each carrying `Will import` or `Rejected` as WORDS beside an intent colour through the shared `StatusBadge` — the word "Imported" is never a verdict, because the backend has imported nothing and the app must not claim it has. The two per-row conventions are the epic's compliance split: a will-import row reads like a listed request (account number masked, NO reveal, plain-language transaction type), a rejected row reads like `RejectedRows` renders one (the file's own values untranslated, masked with a PER-ROW reveal whose accessible name also names the preview, since the Rejected rows section offers its own control for the same row) — no reveal-all anywhere, and the revealed set lives in the loaded state so a re-read starts fully masked. States the two counts in plain language (rows that WILL import, rows rejected). Every way the file can defeat it is a stated problem with NO table at all — a body that will not parse, a column shape that is not this file's, an overlay that cannot be read as rows, and a parsed row count that does not reconcile with the service's `RecordCount` — because a half-drawn table tells the user something false; a refused READ is the separate answer, in the service's own words, with the page's only `Load the preview again`. Nothing here throws. Hosts `CorrectionRowsDownload` over the rejected rows the loaded preview holds (`rowsToFixIn`), which is why the correction download lives in this section and not in the Downloads one. |
| `CorrectionRowsDownload`, `CORRECTION_DOWNLOAD_LABEL` | `web/src/components/files/CorrectionRowsDownload.tsx` | The control that hands the import preview's rejected rows over as a file to correct offline and upload again (`{ rejectedRows: ImportPreviewRow[] }`): renders NOTHING when nothing was rejected — absent, never disabled — and takes no session or role prop, both roles being granted it. Its label (`Download rows to fix and re-upload`) may never contain the word "error", which the service's own diagnostic download owns on this page, and the words saying WHICH file it is are tied to it with `aria-describedby` rather than left floating beside it. The file is built ON ACTIVATION (never a memo — the table it sits above may hold 10,000 rows) and handed over by `deliverFile`, there being no correction endpoint to link to. Building and delivering share one `catch`: a file that could not be produced is stated in the app's own plain words with the same control still there, and asking again clears that statement — the control is never disabled, in that state or while a file is being built. |
| `SubmittedFileActions` | `web/src/components/files/SubmittedFileActions.tsx` | What the Finance Uploader may DO to one submitted file (`{ file: FileLog; actingUploader: string \| undefined; onRetried: () => void }`): `Retry validation` while the file's status is `Validation failed`, and `Delete file` whatever the status — the delete has NO status rule at all, an `Imported` file included, and reinstating one or adding a second wider action beside it is the way this goes wrong. Renders NOTHING AT ALL — no heading, no control — for a session with no acting uploader; absent, never disabled, which is what makes the role exclusion structural, and it is the only reason nothing is rendered. The delete is gated by the shared `DeleteFileConfirmation`, which owns the title, all three states of the description and the two dialog labels — none of that wording is written here, because the Expense files list opens the same confirmation and the two must read identically. The `file` this page was last handed is what that confirmation describes (`useDeleteFileConfirmation([file])`), so the page's own 15s re-read moves the dialog's wording with the file rather than leaving it on a stale snapshot. `Retry validation` is never the bare `Try again` the processing history owns on this screen. A refusal is reported here in the service's own words with the dialog CLOSED and the actions still offered — a user is never held in a dialog to read why nothing happened, nothing claims a success the service did not report, and what the service does to a file that has already imported is unverified, so whatever it answers is what is shown. A confirmed delete sends the reader to the Expense files list (`router.replace`), because the file is inactive afterwards and its own page would only be able to say it is gone. |
| `useDeleteFileConfirmation`, `DeleteFileConfirmation`, `DeleteFileConfirmationController`, `AskedDeletion` | `web/src/components/files/DeleteFileConfirmation.tsx` | The ONE confirmation shown before a submitted file is deleted, shared by the file's own page and every row of the Expense files list — a second dialog built for the second surface, however plausible its wording, is how this goes wrong. A hook plus the dialog it drives: the surface holds one controller (one per table, since only one row can be being CONFIRMED at a time), hands it the files it CURRENTLY has (`useDeleteFileConfirmation(listed)` — the whole table from the list, `[file]` from a file's own page), the trigger calls `ask(file)`, and `<DeleteFileConfirmation confirmation={…} onConfirm={(file) => …}>` renders whatever is being asked. WHAT IS ON SCREEN IS DERIVED FROM THAT CURRENT DATA, never from the snapshot taken at the ask: both surfaces re-read on a 15s timer while any file is in progress, so a file can IMPORT with its own confirmation open on it, and a frozen snapshot is how this dialog would go on saying none of a file's rows will become expense payment requests when they already have and may already be decided (BR5, the same understatement the failed-count state exists to prevent, by another route). A file that has left the caller's data altogether keeps its snapshot, there being nothing newer to describe it with. The count follows the same derivation (`countStateFor`): read ONLY for a file that reads as imported, and started from the state that says a count is OWED rather than from `ask`, so a file that becomes countable later is counted from that moment. Nothing is set synchronously in that effect — the state changes only when the read ANSWERS — so it is not the `set-state-in-effect` shape, and the counting wording is on screen in the first render after the ask because it is derived rather than stored, never the short "nothing to lose" wording for a moment first. Every ask carries a sequence number no later ask reuses, and an answer is written through a functional update that keeps it only while the state still holds that same ask — so a read whose dialog was closed, re-asked, or re-asked for a DIFFERENT file can neither answer over newer state nor resurrect a dialog the user has left. A refused read becomes its own state carrying the service's own reason (`transactionListFailureMessage`), never a zero and never the short wording. Nothing here deletes anything: `onConfirm` hands the confirmed file back to the caller, which is what keeps the one delete call in one place. The count landing changes only the description's TEXT — nothing re-mounts — so the way out keeps its initial focus and a stray Enter still keeps the file, which is why the in-flight state is announced in the description itself and not in a second live region beside it. |
| `ToastProvider`, `useToast`, `ToastContainer`, `Toast`, `ToastOptions`, `ToastLink`, `ToastAction`, `TOAST_DEFAULTS` | `web/src/contexts/ToastContext.tsx`, `web/src/components/toast/`, `web/src/types/toast.ts` | The app's ONE in-app notification surface, mounted once by the root layout: `showToast({ variant, title, message })` raises one, and the container holds the live ones in a `region` named "Notifications" — OUTSIDE `main`, and the handle anything looking for a notification uses (never a bare `alert` query, which also matches Next's permanently empty route announcer), since the toast's own role varies with its variant (`alert` for `error`, `status` otherwise). Each carries a dismiss control unless `dismissible: false`. An omitted `duration` takes the 5s default and fades; `duration: 0` never fades, which is how state the user must ACT on persists (source UI-19). `link` is how a notification offers somewhere to go: a real anchor rendered inside it (tab-reachable, announced as a destination, openable in a new tab), and following it dismisses the notification. `action` is the same idea where there is nowhere to GO: a real `<button>` rendered inside it, for something the user must DO on the spot, and taking it dismisses the notification too — so a report can never go on offering to act on something already acted on. Pair it with `duration: 0`. `onClick` on the toast body predates both and must not be used for anything the user has to act on — a clickable non-link is not keyboard-operable. |
| `deliverFile` | `web/src/lib/files/deliverFile.ts` | The one way bytes the app has received are handed to the user as a saved file: a blob address activated through a `download` link, under the name passed in (the SERVICE's own name — a blob address carries none, so without this the user gets a random one), released on the next task because revoking in the same one can cancel the transfer. Also the way a file the app BUILT reaches the user (the CSV export's blob), so no screen writes this again. |
| `DELETE_FILE_LABEL`, `CONFIRM_DELETE_LABEL`, `KEEP_FILE_LABEL`, `deleteConfirmationTitleFor`, `NEVER_IMPORTED_MESSAGE`, `COUNTING_REQUESTS_MESSAGE`, `COUNT_UNAVAILABLE_MESSAGE`, `countUnavailableMessage`, `importedConfirmationMessage`, `RequestCounts`, `RequestCountState`, `NOT_COUNTED`, `COUNTING`, `countStateFor`, `deleteConfirmationMessageFor`, `mayHaveProducedRequests`, `requestsProducedBy`, `countRequests`, `fetchRequestCountsFor` | `web/src/lib/files/deleteConfirmation.ts` | Everything said before a submitted file is deleted, and the counting that wording rests on — stated once because two surfaces (the file's own page and the Expense files list) must say it identically. THREE states, never two: a file that has IMPORTED gets its real numbers (how many requests it produced, how many already approved and rejected, and that the record of who decided them goes too), any other status gets the short warning with NOTHING read for it, and an imported file whose count could not be read gets its own state — which never softens into the short wording, never renders as a zero, and carries the service's own reason appended. A genuine none is stated as none and shares no wording with that failed state, so the two can never be mistaken for each other. Which state applies is decided from `CurrentStatus` as the service reported it, never inferred from another signal — and `countStateFor` reads it against what the service says NOW rather than what it said when the delete was asked for, so a file that imports under an open confirmation stops being described by the status it has left and is counted instead (BR5). The count is the service's own transaction rows, NOT the file's self-reported `RecordCount`: the whole `GET /v1/transactions` set is read (the endpoint takes no parameters) and narrowed on `FileLogId` in the browser. `RequestCounts` is declared here rather than imported from the mock factory that counts the same shape — `src/lib` never imports from `src/mocks`. The three control labels live here too, all different (`Delete file` asks, `Delete the file` does it, `Keep the file` backs out) and none of them reading "Cancel". Each state is ONE sentence-run, because `ConfirmAction`'s description is a single string. Nothing here deletes anything. |
| `FILE_ID_PARAM`, `submittedFileAddress` | `web/src/lib/files/fileAddress.ts` | Where one submitted file's own page lives, stated once for every surface that sends a user there (a file's row, and any notification about a file): the registered address plus the file's identifier in the query, spelled `LogId` as the transactions contract spells it. |
| `FILE_SUBMITTED_EVENT`, `announceFileSubmitted`, `subscribeToFileSubmissions` | `web/src/lib/files/fileSubmissions.ts` | How "a file was just submitted" travels between two independent components on one screen, so the file list can re-read itself without the submit form owning it. Safe to call where there is no browser. |
| `UPLOAD_FILE_COLUMNS`, `SubmittedFileColumn`, `SubmittedFileRow`, `TOLERATED_TRAILING_COLUMN`, `UNREADABLE_BODY`, `UNEXPECTED_COLUMNS`, `SubmittedFileReadProblem`, `SubmittedFileReadResult`, `parseSubmittedFileCsv` | `web/src/lib/files/parseSubmittedFileCsv.ts` | The only CSV READER in the app — the symmetric counterpart of `lib/transactions/exportCsv.ts`'s writer, under the same minimal RFC 4180 rules, so anything the app writes it reads back unchanged (a comma, a doubled quote or a `\r\n` inside a quoted value survives; nothing is trimmed, coerced or normalised, because every value is text and a row rejected for a non-numeric amount holds exactly what the person typed). Takes CSV TEXT, not a `Blob` — the caller reads the bytes (with `FileReader`; jsdom implements neither `Blob.text()` nor `Blob.arrayBuffer()`). States the SEVEN upload columns of `documentation/transactions_2026-04-15.csv` once, from which both the shape check and the position each value is read from derive — never `EXPORT_COLUMNS`, which is the unrelated nine-column hand-over report. Answers one record per data line in file order (the header row is never a record, a trailing newline begins no empty one), or an explicit refusal carrying NO rows at all — never a throw and never the lines it read before the file went wrong. The two refusal reasons are kept apart because they are different things to tell a person: `unreadable-body` (a quoted value never closed, a line whose fields do not line up with the header, nothing readable at all) and `unexpected-columns` (reads fine as CSV but is not this file). EXACTLY ONE extra column is tolerated and discarded: a trailing `Reason`, in last position, matched exactly — a deliberate round-trip affordance so the correction CSV re-uploads unmodified, not a lax parser, so an unknown extra column or a ninth alongside `Reason` stays a refusal. Reads a chunk of lines then hands the main thread back through a `setTimeout(0)` MACROTASK (a microtask yield cannot be painted between), which is why it is async: at the 10,000-row ceiling the file is downloaded and parsed in the browser. |
| `APP_OWNED_DEFECT_WORDING`, `NO_REASON_GIVEN`, `defectWordingFor`, `defectWordingsFor`, `DEFECT_WORDING_SEPARATOR`, `defectWordingLine` | `web/src/lib/files/defectWording.ts` | What a user reads about ONE rejected row's defect, stated once for every surface that shows one (the `Rejected rows` section, the import preview, the correction CSV). The two sources are never mixed: this app's four fixed sentences selected by `ErrorColumn` (`Reference`, `Amount`, `TransactionDate`, `Currency`), for which the service's own text never reaches the user; the service's `ErrorMessage` verbatim for everything else, including `TransactionType`, which the app holds no accepted-value list for and never judges. A `Map`, not an object literal, because `ErrorColumn` came out of parsing an untrusted payload and an inherited property (`toString`) must not answer as wording. `undefined` means the row carries no defect signal at all — say `NO_REASON_GIVEN`, never a guessed reason. A row the service reported SEVERAL defects for says all of them: `defectWordingsFor` resolves a row's entries to its distinct sentences in the service's own order (someone correcting the file has to fix every one of them), and `defectWordingLine` joins them for a surface with room for only one line of text (the correction file's `Reason` cell), so the screen's list and the file's cell start from the same answer. |
| `REJECTION_MATCH_KEY`, `ImportPreviewVerdict`, `ImportPreviewRowSource`, `ImportPreviewRow`, `ImportPreviewRows`, `importPreviewRows` | `web/src/lib/files/importPreviewRows.ts` | The join between a submitted file's parsed lines and the rows the service rejected: one row per line IN FILE ORDER carrying `will-import` or `rejected` (never `imported` — the service has imported nothing), plus the counts. `REJECTION_MATCH_KEY` (`Reference`) is the epic's WORKING ASSUMPTION about what ties a rejection back to its line — the validation-errors wire shape is inferred, not documented, so a live response carrying a positional or identity key instead is a one-line change here and nowhere else. The key cannot match every row and that is not a bug ("Reference missing" is itself a rejection reason): a rejection matching no line is listed ONCE as a rejected row in its own right, from the values and reason the payload carries — never dropped, never duplicated, never attributed to a line it might not belong to. UNMATCHED means the key matches NO line, never "that line is already spoken for": each line carrying a key is claimed once in file order, and a further rejection carrying that key is ANOTHER DEFECT on a line already listed (a row whose amount is not a number and whose currency is not supported), attached to it rather than made a second row — which is what keeps `willImport + rejected` equal to the file's rows plus the genuinely orphaned rejections, and keeps the correction file from carrying the same line twice. Correct whether the service reports one entry per row or one per defect, which is still unverified. A row therefore carries its entries and its `reasons` as LISTS. An empty key is no key. Nothing is translated or reformatted here (every value stays the text its source held); each rejected row's reasons come from the shared `defectWording.ts`, so the preview and the correction CSV cannot explain the same defect two ways. `source` says which of the two a row's values came from, which is what lets the correction CSV write a matched row from the file's own line and an unmatched one from the payload. |
| `CORRECTION_REASON_COLUMN`, `CORRECTION_COLUMNS`, `CORRECTION_MEDIA_TYPE`, `correctionHeaderRecord`, `correctionRecordFor`, `rowsToFixIn`, `buildCorrectionCsv`, `correctionFileName` | `web/src/lib/files/correctionCsv.ts` | The file of rejected rows a user corrects outside the application and re-uploads as a new submission, built in the browser from the preview already on screen. EIGHT columns, the eighth always LAST: the seven upload columns in the upload's own order, then `Reason` — generated from `parseSubmittedFileCsv.ts`'s `UPLOAD_FILE_COLUMNS` and its `TOLERATED_TRAILING_COLUMN`, so what this writes and what that reader accepts back are one statement and the download → correct → re-upload loop cannot dead-end. Whether the upload tolerates that eighth column is UNVERIFIED, and the trailing position is the mitigation: a positional consumer is unaffected, and dropping the last entry of `CORRECTION_COLUMNS` is the whole of the fix if the service refuses it. Never `EXPORT_COLUMNS`, the unrelated nine-column hand-over report. Account numbers are written WHOLE — the same documented compliance exception the expense export carries, and the one place this epic's file and its own screen deliberately disagree (the preview masks them). Nothing is re-derived: which rows are rejected, where each row's values came from and what is wrong with it are `importPreviewRows.ts`'s answers through the shared `defectWording.ts`; a row rejected for more than one thing carries EVERY reason in its one `Reason` cell (`defectWordingLine`), and a row the service gave no defect signal for says so rather than leaving an empty cell. Escaping is minimal RFC 4180 and construction yields between chunks (hence async), exactly as `lib/transactions/exportCsv.ts` writes. The file name says what it holds plus the day and time of day from the reader's own clock, dash-separated, and carries none of the wording the service's own error file owns. |
| `readBlobText` | `web/src/lib/files/readBlobText.ts` | The text inside bytes the app has just downloaded — the counterpart of `deliverFile`. It is a `FileReader` deliberately and must stay one: jsdom implements neither `Blob.text()` nor `Blob.arrayBuffer()`, so the obvious call passes in a browser and throws in every test that renders a screen reading a downloaded file. Decodes as UTF-8. |
| `SESSION_IDLE_TIMEOUT_MS`, `SESSION_WARNING_LEAD_MS`, `SESSION_TIMED_OUT_TITLE`, `SESSION_TIMED_OUT_MESSAGE` | `web/src/lib/session/config.ts` | The session's timings and the one sentence a user gets when it ends (shared by the idle path and the service-says-it-is-gone path). Timings come from `NEXT_PUBLIC_SESSION_IDLE_TIMEOUT_SECONDS` / `NEXT_PUBLIC_SESSION_IDLE_WARNING_SECONDS` in seconds and are converted to milliseconds here only; the warning lead can never exceed half the idle period. No absolute session cap is asserted anywhere — the auth service owns that. |
| `IdlePhase`, `IdleSessionStatus`, `subscribeToIdleSession`, `idleSessionStatus`, `idleSessionStatusOnServer`, `subscribeToIdleExpiry`, `keepSessionAlive` | `web/src/lib/session/idleSession.ts` | How long the user has been idle, as external state React watches (`useSyncExternalStore`) plus a separate subscription for the moment it runs out. Decides everything by comparing `Date.now()` against the last activity, so a slept laptop or throttled tab reads correctly; watching starts the clock and the last watcher leaving stops it. On the first watch of a document nobody has touched (no `first-input` entry) the count starts when the page arrived rather than when React started up. |
| `touchSession` | `web/src/lib/session/sessionApi.ts` | Confirms the session is still alive with the auth service (`GET /v1/auth/userinfo`) from the browser; a 401/403 is the service reporting it gone. |
| `SessionTimeoutWarning` | `web/src/components/session/SessionTimeoutWarning.tsx` | The signed-in shell's idle warning: an `alertdialog` with a live countdown and one action that touches the session, plus the return to the sign-in screen (with the shared plain explanation) when the idle period runs out or the service says the session is already gone. Mounted once by the `(authenticated)` layout; renders nothing while the user is active. |

## Conventions

- The browser only ever calls the app's own address. Proxy route handlers at `app/v1/auth/[...path]/route.ts` and `app/transactions-api/[...path]/route.ts` forward those paths to the two services, resolving each address per request via `lib/api/serviceBaseUrl.ts` — never from a literal in application code, and never through a `next.config.ts` rewrite (a rewrite destination is resolved at `next build` and frozen into `.next/routes-manifest.json`, so the running server's configuration would be ignored). A new backend service gets its own mount folder and route handler, exporting only the verbs its spec defines and forwarding through `forwardPathToService` so the caller's path cannot leave the mount point.
- An operation whose audit identity must come from the SESSION gets its own route handler under `app/api/…` (the first is `app/api/decisions/route.ts`) instead of travelling through a `[...path]` proxy mount, which forwards caller headers verbatim and would let a browser name anyone as who did it. Such a handler resolves the identity itself with `fetchUserInfo` (`requireSession()` redirects, which is not an answer a fetch can act on), enforces the role, forwards through `forwardToService` with headers it composes itself, and passes the service's own status and body back. The proxy mount then refuses those service paths at the PATH level (404, nothing forwarded) — never by stripping the audit header globally, which would break the file cancel that legitimately sends one through it. An audit name that cannot go in a header at all (a header value is a byte string, so any code point above U+00FF — Cyrillic, CJK; accented Latin is fine) is reported as a bodyless 500 with the reason logged server-side, and the call is not sent: the name is never re-encoded, transliterated or stripped to fit, because what gets recorded as who acted is the service's contract, not the handler's to reinterpret.
- Authentication is cookie-session only: no token or credential in browser-reachable state, no auth header from the API client. Server-side calls forward the incoming `session` cookie explicitly.
- Authorisation is re-resolved per server-rendered navigation from `GET /v1/auth/userinfo`; nothing caches roles.
- Test/mock factories in `web/src/mocks/data/` re-export the production types and role constants rather than declaring their own, so mocks cannot drift from the app's contract.
- Forms compose the Shadcn `form` primitive with react-hook-form and a Zod schema from `lib/validation/schemas.ts`. Required-field checks report on blur (`mode` and `reValidateMode` both `'onBlur'`, `noValidate` on the `<form>`), server-checked rules on submit; nothing reports on a keystroke. Required fields carry an `aria-hidden` asterisk in the label plus one legend line per form.
- A failed request's message is shown to the user only when it came from the service (`serviceMessageOf`); otherwise the screen supplies its own plain wording.
- Every screen needing a session is a page under `web/src/app/(authenticated)/`. That group's layout is the only gate (`force-dynamic`, `await requireSession()`) and renders the shared header; a new protected screen adds a page there and re-gates nothing.
- An address with no screen inside the signed-in area is answered by `app/(authenticated)/not-found.tsx` — the group's own `not-found` boundary, so the group's layout (and therefore the header and its navigation) stays on the page and the user is never stranded with only the browser's Back button. Next still answers it 404. A nested segment must not add a `not-found.tsx` of its own unless it needs different wording; `notFound()` from any page in the group already lands here. An address matching no route at all lands here too, via the group's catch-all `app/(authenticated)/[...unmatched]/page.tsx`, which does nothing but call `notFound()` — so a mistyped address keeps the shell instead of falling through to Next's root fallback (which renders outside every layout). A real screen always wins over it, so adding one needs no change there; the cost is that a signed-out visitor to an unknown address is sent to sign in rather than getting a bare 404.
- The root layout renders no `<main>`. Each screen supplies its own, so the app header stays a sibling of `main` and keeps its `banner` landmark.
- Signed-in screens are FULL WIDTH and left-aligned — no width cap and no centring on the `(authenticated)` layout's `<main>`, whose screens are dense tables that a centred column would starve. The only horizontal space is padding, and the header's inner container carries the SAME padding with no cap of its own: the app's name and the content beneath it start on one vertical line, so changing either one without the other visibly misaligns the shell. A screen that needs a narrower measure constrains its own content, never the layout. An element that has to run FULL-BLEED cancels that padding with a negative margin and re-applies the same padding inside itself (`-mx-4 px-4`, plus `-mt-8` where it also has to reach the top edge — the control block, the narrowing strip, the line of actions on it and the applied-narrowing summary all do this), so its own content still lines up with the header and the rows while its rules reach the edge of the page; the padding is never taken off the layout, which every other screen depends on.
- Header/user menus are non-modal (`modal={false}`): a modal Radix menu marks the rest of the page `aria-hidden` while it stays focusable, which fails the accessibility scan.
- Authorisation for a screen lives in `lib/auth/access-map.ts` and nowhere else. An epic shipping a screen registers or edits its address there, and its page returns `<PermissionDeniedMessage deniedPath={…} />` when `canAccess(session, path)` is false, before rendering anything — no second gating mechanism, no client-side gate. An address is registered before its screen exists so a denial can never fall through to not-found.
- An address is withheld from a role only when that role may not READ the screen at all. Every registered address is open to both project roles, because §6.5 grants both of them read on what these screens show; what only one role may DO (submit a file, decide on a request) is a role check inside the screen, on that control. So the account the access map actually excludes is one whose role this project does not recognise — `hasRole` grants an unknown name nothing, and `RoleEntryPoints` / `HeaderNav` answer that case explicitly. Anything asserting exclusion (hidden-never-disabled, the in-page denial) is exercised with `userInfoWithUnrecognisedRole()` from `src/mocks/data/identity.ts`, which the Playwright auth stub can also mint a session for.
- Screens offer only what the current roles allow, as real links carrying an `href` (never a button that pushes a route), and an excluded action is left out of the markup rather than rendered disabled.
- Getting between screens is the header's job and needs no work from a new epic: `HeaderNav` offers exactly `entryPointsFor(session)` — the same facts, and the same wording, as the landing screen's entry points — so registering an address in `lib/auth/access-map.ts` is all it takes to appear in the navigation.
- Nothing in the header is hidden at any width — no collapsed menu, no CSS-hidden control, no width-branching in JavaScript. Every `a[href]`/`button` inside the `banner` has to be Tab-reachable and paint a visible focus indicator (epic 1's keyboard sweep walks all of them at desktop width), and the navigation's destinations have to be in the markup for a session's permitted set to be assertable. A header that outgrows one row wraps onto a second.
- An action that has to be agreed to before it happens goes through `components/common/ConfirmAction.tsx` — never a second dialog of its own. The caller supplies a title naming the object, what accepting does, and three phrases that do not read alike (asks / does it / backs out); the way out holds focus, nothing is sent until the confirming choice is taken, and the dialog CLOSES as the answer is given so a refusal is read on the screen behind it rather than inside a dialog the user is held in.
- Where one dialog hands straight over to another (a step that collects something, then the confirmation), the first suppresses Radix's focus restore on the way out — `onCloseAutoFocus={(event) => { event.preventDefault(); }}`, and only for that case. Radix returns focus to whatever was focused before a dialog opened, on a `setTimeout` as it unmounts, which lands AFTER the second dialog has focused its own control and pulls focus back onto the list behind both. Backing out instead of handing over must still restore normally, or the keyboard user is left on `body`.
- A control that DISAPPEARS as the result of the action it was just used for has to hand focus to a surviving control on the same object, before or as it goes. Removing a focused element drops focus to `<body>`, so a keyboard user is put back at the top of the page after every action — the same harm as disabling a control under them (see `FileDownloadActions`). The hand-off is a real one between two elements the component owns (a ref), never a search of the page for a button by its wording, and it fires on the commit that removes the control: any dialog in the flow restores focus to its own trigger on a `setTimeout` as it unmounts, so a hand-off made earlier is simply undone.
- The Shadcn `alert` default variant is the one used, with its description set to `text-foreground` and its title's one-line clamp released: the destructive variant paints the description at reduced opacity, which misses AA contrast for wording the user must act on.
- Every colour, the type face and the corner radius live in `web/src/app/globals.css` as raw-hex tokens, with a value in both `:root` (light) and `.dark`. Beyond the Shadcn set it declares `--success` / `--warning` / `--info`, `--destructive-foreground`, `--brand-accent` / `--brand-tertiary` (each with a paired foreground) and `--overlay` (the dim behind a modal), so a status, brand or modal surface never needs a Tailwind palette utility or a colour keyword. Components carry no colour value — including the CLI-generated primitives in `components/ui/`. A status is shown through the shared `StatusBadge` (see its row), which pairs the status TEXT and a drawn shape with one of five intents, as INK rather than as a filled chip — in progress or informational on `--info`, finished well on `--success`, needs attention on `--warning`, refused on `--destructive`, inert or unrecognised on `--muted-foreground` — never colour alone. A screen supplies what its own statuses MEAN, never a colour.
- Filled brand and status surfaces take the theme's own text colour as their foreground, not white: the brand blues are light, so `bg-primary text-primary-foreground` clears 4.5:1 in both themes only that way. `--input` is darker than `--border` so a field's outline clears 3:1 (WCAG 1.4.11) — load-bearing wherever a field is marked by an underline ALONE (the request list's narrowing strip), since there is then no other outline to carry it.
- Motion the app owns is declared in `globals.css` — a `@keyframes` plus an `--animate-*` theme key, reached as a Tailwind `animate-*` utility — never as an inline style or a per-component duration. Two rules make it work WITH the global `prefers-reduced-motion` block there (which forces every duration to `0.01ms`): the `to` state is the element's own natural state and the animation carries NO fill mode, so under that preference the animation simply ends immediately on the value and position the animated path lands on. A component must not add a competing reduced-motion declaration of its own — one information design at two paces, not two implementations. Anything animated inside a figure or a row is animated on a CHILD, and by transform only, so the box a reader (or a layout-shift measurement) sees never moves. Motion that means "the data moved" is triggered by the DATA moving, not by the figure's value moving: a figure describing a narrowed set changes as the reader types, so the animation is driven by comparing the whole set's own value against the narrowed one, and a narrowing re-states the figure with no motion at all.
- The light/dark version is expressed as the `dark` class on `<html>` and is resolved by the blocking inline script the root layout embeds in `<head>` — before first paint, never in an effect. A new screen needs no theme work; a new provider stack must not be nested around the root layout to add one.
- Browser state that already exists before React runs (the theme class, storage, a media query) is watched with `useSyncExternalStore`, never copied into React state by an effect: the lint rule `react-hooks` rejects setting state inside an effect, and a copy can disagree with what is on screen. Where the server cannot know the value, the server snapshot is the honest "not known yet" and the render handles it.
- The app's TWO faces are loaded once by `next/font` in the root layout as CSS variables (`--font-public-sans`, `--font-azeret-mono`), both variable across the full 100-900 weight axis, and reached only through the `--font-sans` and `--font-mono` tokens: Public Sans sets every word, Azeret Mono the fixed-field notation (figures, references, masked account numbers, control totals, field labels). No component names a font family, so a face swap is a change to the root layout and the token layer alone.
- The design's direction contract (seed key `29469d17`) is an HTML comment emitted as RAW MARKUP by an inert `hidden` wrapper at the top of `<body>` in the root layout, and must survive the production build (`grep -rl "29469d17" web/.next` non-empty). It cannot move onto the `<body>` element itself to gain the one position ahead of it: React forbids children beside `dangerouslySetInnerHTML`, so the app's content would have to render outside `<body>`, and React then re-applies that markup on the next client re-render — `router.refresh()`, which runs on sign-in, sign-out and session timeout — emptying every rendered screen out of the document. Next.js's own streaming-metadata element is the only thing ahead of the wrapper, on every page.
- A choice from a fixed set is the Shadcn `select` (Radix), never a native `<select>`: the native option list is drawn by the operating system, so a keyboard-only browser test cannot focus an option, and the project's WCAG 2.2 AA keyboard-completability bar cannot be evidenced. Its trigger takes the `id` the `FormLabel` points at. Outside a form the trigger takes that `id` from its own `Label` and also `aria-labelledby` naming it: the trigger is a BUTTON, so its accessible name would otherwise be whatever value it currently displays, leaving the control unnamed for assistive technology and unaddressable by name in a test. Its option list opens with an animation and closes without one: Radix takes the whole rest of the page out of the accessibility tree while the list is open (`aria-hidden` everywhere else, its stand-in for `aria-modal`) and puts it back only when the list UNMOUNTS, so a closing animation leaves the page unreadable for as long as it plays — after the choice has been made and while the screen is already showing its answer.
- A sortable table column is a `columnheader` carrying `aria-sort` (`'none'` on every column but the one in force) with a real `button` named for the column inside it, and the direction is in that button's accessible name as well as in its arrow. A clickable `<th>` cannot be reached by keyboard at all, and an icon-only indicator leaves the state to eyesight, which R15 forbids.
- Controls that move between pages are `button`s carrying `disabled`, never anchors with `aria-disabled`: paging changes no address, `disabled` is what makes "cannot be used" programmatically determinable, and it takes the control out of the tab order so a keyboard sweep never lands on one that does nothing. Shadcn's `pagination` primitive is used for its landmark and list markup only — its own previous/next slots are `<a>`s — and its nav is labelled for what it pages rather than left as the generic "pagination", since the signed-in shell already has a navigation landmark.
- An icon-only control carries a Shadcn `tooltip` AND an `aria-label`, both built from the same string. The tooltip is what reveals the wording on hover *and* on keyboard focus; a native `title` never shows on focus, so it leaves a keyboard user with nothing. The two must match exactly — a sighted user and a screen-reader user being told different things is the defect the pairing exists to prevent. A control that has visible wording of its own needs neither. `TooltipProvider` is context only (it renders no element) and wraps whichever region holds the controls; Shadcn's `Tooltip` does not supply one. It turns Radix's hoverable content OFF, so the wording goes away the moment the pointer leaves the control: left on, Radix draws a grace area between trigger and content instead and waits for a LATER pointer move outside it, so a pointer that leaves in one movement and stops leaves the wording on screen indefinitely. A tooltip that has to be pointed INTO is the wrong primitive — that is a popover.
- Where a narrow screen needs a genuinely different presentation (not a re-flowed one), the two are branched in JavaScript on the watched media query (`lib/layout/viewport.ts`), never rendered together with one hidden by CSS. A CSS-hidden duplicate is still in the accessibility tree's reach, still doubles every value on the page for anything reading by role or by text, and still costs the render.
- Every per-request action in the expense request list is a DIRECT control on the row and on the card; there is no ⋯ action overflow menu anywhere on a request. This deliberately supersedes the MECHANISM `expense-request-list` R16 named (an action overflow at phone width) — approved by the user at manual test — while still meeting what R16 is for: every action reachable at every width, now in one activation. Do not restore the menu as a missing feature.
- A figure a reader has to be able to ADDRESS carries its own visible label as its accessible name (`aria-labelledby` at the label, so exactly one element carries the wording) and holds the figure and nothing else — that is what lets a screen reader read "Awaiting decision, 5" and a test say the figure IS 5 rather than that the screen mentions a 5 somewhere. Where such a figure also MOVES with no user action (a self-refresh bringing in a colleague's work), it is additionally a polite live region — and then the labelled element cannot also be the group it belongs to, because an element has one role and two elements answering to the same name leave neither addressable. The pair is a `<fieldset>` named NATIVELY by its `<legend>` (the visible label) holding the `role="status"` figure: one aria reference, one element carrying the value. `aria-label` on the live region is specifically wrong — it would freeze the region's name over its changing contents. Where such a figure is derived from a NARROWED set, it is live only for movements of the underlying data: a narrowing re-states it with `aria-live="off"` for that one commit (the role stays, so it is still the element a reader and a test address), because a region left permanently live over a narrowed figure queues an announcement per keystroke in the search box.
- A row (or card) in a list that re-renders on every keystroke is memoised on stable props — the record itself, plus one callback that TAKES the record rather than a closure built per row. That is what keeps narrowing responsive at the volume ceiling; a callback rebuilt each render silently defeats it.
- A scrollable wrapper (`overflow-x-auto`, e.g. the Shadcn `table` container) must hold keyboard-focusable content or carry `tabindex="0"` itself: axe's `scrollable-region-focusable` is tagged `wcag2a`/`wcag211`, so a wide read-only table with no control in it fails the accessibility scan.
- A choice made in one gesture — picking from a listbox, picking a file — is validated as it happens (`setValue(…, { shouldValidate: true })`), not on blur: the convention above forbids reporting on a KEYSTROKE, and a finished selection is not one. This is what lets a wrong file be refused before any request is made.
- jsdom implements no `window.matchMedia`, so `vitest.setup.ts` supplies one that matches nothing (i.e. the light theme). A test that depends on a particular media query stubs it for that test only.
- jsdom also implements no Pointer Capture API and no `scrollIntoView`, both of which Radix uses to run a listbox, so `vitest.setup.ts` supplies honest do-nothing stand-ins (guarded, reporting jsdom's true state, swallowing nothing). Any test rendering a `select` / `dropdown-menu` / `popover` needs them; without them the first click on a trigger throws.
- Testing Library's async utilities (`findBy*`, `waitFor`, `waitForElementToBeRemoved`) run on a 5s budget set once in `vitest.setup.ts` (`configure({ asyncUtilTimeout: 5000 })`), not the library's 1s default — the companion of the 15s `testTimeout` in `vitest.config.ts`, and set below it so a missing element still fails as "Unable to find role=… " with the DOM printed rather than as a bare test timeout. 1s is wall-clock in a run where 43 jsdom files share 12 cores: measured on the `/upload/file` preview chain, the same wait was 65ms in isolation and 338–1201ms in the full run. It is a ceiling and changes no expectation — but it is not a substitute for waiting properly: a test hangs its wait off an observable "the screen has settled" signal (a loading `role="status"` gone, the rows present) and then queries, rather than betting one query against a whole asynchronous chain.
- Playwright alert queries are scoped to a region (e.g. `getByRole('main').getByRole('alert')`): Next.js renders its route announcer as a permanently empty body-level `role="alert"`, so an unscoped query always matches two elements.
- The `(authenticated)` layout notifies the user through the root layout's `ToastProvider`, so a test rendering that layout renders it inside the provider + `ToastContainer` composition the running app always has around it.
- A message the user is given twice — once as a notification when something happens, once as text on the screen they land on — is a live region only the first time: the copy on the destination screen takes `role="note"` so a screen reader is not told the same sentence again.
- A view that keeps itself current re-reads its OWN call on an interval, only while something it shows is still in progress, and stops once nothing is — one interval at most, tied to that single fact and cleared on unmount. A re-read that fails changes nothing on screen: the last known values stay, and the failed-load state is reserved for a read that left the user with nothing (`setState(current => current.phase === 'loaded' ? current : failed)`). "Something just finished" is told from the previous status per record id, held in a ref, so a record already finished on the first read is not announced. Where a screen has SEVERAL sections that must catch up together, the one that knows whether anything is in progress owns the single interval and hands the others a changing signal prop to re-read on — never one timer per section, which would drift and multiply the load. An action whose answer says nothing about the new state (a retry) uses that same signal rather than a mechanism of its own — and, because the record may still report the state it had before the action (or already report it again), such an action ALSO keeps the interval running for a fixed number of further reads: an accepted action is itself a reason to keep asking, so the outcome cannot depend on catching an in-progress status in one sample. Bounded, so the view still goes quiet. WHERE WHAT IS BEING WATCHED IS OTHER PEOPLE'S WORK rather than a record finishing, there is no idle state to stop at, so the interval runs for as long as the view is open (the expense request list). Everything else above still holds; what replaces the stop condition is a gate on the document being visible (`lib/layout/pageVisibility.ts` — nothing is asked of a service while nobody is looking at the answer, and a reader coming back is read for straight away) plus a pause around any action of the reader's OWN that the view must not race. A read that lands then merges over what is on screen rather than replacing it (`lib/transactions/refreshing.ts`), and says what it brought on one polite live region — never a notification, a dialog or anything the reader has to dismiss.

- A section whose state is per-item — one wait, one failure, one answer per thing on offer — keys that state by the item rather than holding one shared state, whenever two of them can be in flight at once (which is the case wherever a control is deliberately left usable while its own call is out). One shared state lets whichever answers first clear or overwrite what the other reported. An in-flight guard is not the alternative: a control that looks usable must never be a silent no-op.

## Cross-epic debt

- CORS on both backends is an open backend requirement (project.md NFR-base-6). The same-origin proxy route handlers make it a non-issue for browser calls; any future direct browser-to-service call would reintroduce it.
- There is still no root `app/not-found.tsx`. Nothing inside the app reaches Next's built-in fallback any more (the signed-in group's catch-all answers unmatched addresses), so this only matters if a future public area outside that group needs its own not-found. A root boundary has no session to render a header from, so it stays separate work.
- An ad-hoc `npm run test:e2e` REUSES a dev server already listening on :3000 (`reuseExistingServer`), and one started by hand points at the real auth service rather than the run's stub — so every protected-screen spec is sent to sign-in and fails on "element(s) not found", whatever the spec was about. Stop the hand-started server, or use the epic-end form (`E2E_PROD=1`, port 3100, never reused). Nothing in the app is at fault when a whole suite fails this way.
- Against a COLD `next dev` server the sign-out specs can exceed their 5s navigation window while the proxy route handler compiles on demand; they pass on the config's one retry, and pass first time once the dev server is warm or in `E2E_PROD=1` mode. Dev-server artifact only — nothing in the app is slow in production.
- `web/src/components/ui/alert-dialog.tsx` and `dialog.tsx` (Shadcn CLI output) have their overlay changed from `bg-black/50` to the `bg-overlay/60` token; regenerating either primitive will reintroduce the colour keyword.
- The three expense-request-list tests that drive the narrowing controls — story 2's applied-summary test, both of story 3's range tests and story 4's page-controls test — can each exceed Vitest's 5s per-test timeout when the whole suite runs in parallel workers on a loaded machine (each types into several controls and re-renders the list per keystroke: ~2s in isolation, ~3x that under contention; which of them times out varies run to run). Not caused by any one story — verified on a tree with story 6 stashed, where the same three fail — and all of them pass in isolation and on a suite run that is not competing for cores. Nothing in the app is slow; if one fails a gate, re-run the file on its own rather than reshaping the component. The underlying squeeze is the template's 5s default `testTimeout` against whole-screen integration tests (logged in `template-feedback.md`).
- `web/src/components/ui/select.tsx` and `tooltip.tsx` (Shadcn CLI output) carry deliberate BEHAVIOUR edits, not just styling ones: the select's option list has no closing animation, and the tooltip provider turns hoverable content off (both explained in the file and in Conventions). Regenerating either primitive will reinstate the CLI default and, with it, a page that is briefly unreadable to a screen reader after a filter choice / a tooltip that outlives the pointer. Radix's dialog has the same aria-hidden-until-unmount behaviour behind its own closing animation; nothing observes it there, because closing a dialog returns focus and attention to the page rather than to an answer that has already changed underneath.
- `ImportPreview` renders EVERY parsed row of a file at once, with no paging or virtualisation. At this project's endorsed volume ceiling (10,000 rows) that is a heavy table — the reading half of the epic's NFR-1, which allocated only the download-and-parse half to a story (`parseSubmittedFileCsv` yields between chunks). Ordinary files are unaffected; a ceiling-sized one needs the paging the expense request list already applies to a full-volume client-side list.
- `web/src/components/ui/button.tsx` (Shadcn CLI output) hard-codes `text-white` on its destructive variant. Kept as-is because white clears 4.5:1 on the destructive red in both themes, where `--destructive-foreground` would not on the `dark:bg-destructive/60` tint; regenerating the primitive will reintroduce it. No app code uses that variant.
- The Shadcn `badge` primitive is deliberately NOT installed: its unconditional `rounded-full` capsule is the pill the shared status mark replaced, so re-adding it would be a way back to a chip surface this design has no place for. A short phrase paired with a shape and an intent colour is `StatusBadge`'s job.
